## 1. 配置与文档基线

- [x] 1.1 梳理根目录、`apps/api`、`apps/miniapp` 和 `apps/admin` 实际读取的环境变量。
- [x] 1.2 补齐 `.env.example` 与应用级环境变量样例，覆盖 API 地址、后台 token、微信登录、CPS、AI 审核、数据库和代理配置。
- [x] 1.3 更新 README 或新增内测运行说明，纠正过时的 foundation、主 tab 和能力范围描述。
- [x] 1.4 增加配置就绪检查，识别缺失变量、占位值、真实密钥误提交风险和小程序真机调试 API 地址配置问题。

## 2. 数据准备与复位策略

- [x] 2.1 定义内测测试用户、测试日报、测试社区内容、测试补给项、模拟订单和审核队列数据的稳定标识。
- [x] 2.2 编写开发/内测数据准备说明或脚本，确保只在开发、测试或演示环境运行。
- [x] 2.3 补充数据复位说明或脚本，隔离上一轮审核状态、订单状态、通知状态和本地缓存污染。
- [x] 2.4 检查小程序和后台对空状态、真实 0 数据、无结果、API 错误和本地降级状态的展示边界。

## 3. 总 smoke 验证入口

- [x] 3.1 新增总 smoke 脚本，默认复用现有纵向 `verify:*` 命令并保持确定性。
- [x] 3.2 在根 `package.json` 新增总 smoke 命令，例如 `verify:mvp-beta-readiness` 或最终确定的命名。
- [x] 3.3 为总 smoke 增加跨链路断言，覆盖首跑、内容、CPS、后台、配置和数据状态关键闭环。
- [x] 3.4 为总 smoke 增加敏感字段黑名单检查，避免输出后台 token、微信身份、聚推客密钥、完整 `sid`、原始订单同步响应或 AI 原始 prompt。
- [x] 3.5 确保总 smoke 失败时以非零状态退出，并输出失败链路、可能配置原因和对应纵向验证命令。

## 4. 首跑与内容链路稳定化

- [x] 4.1 验证小程序首跑登录、建档、工作档案保存和首页已摸金额计算链路，修复阻断内测闭环的问题。
- [x] 4.2 验证 API 不可用但存在合法快照时的首页降级展示，修复伪造 ready、演示身份或演示金额风险。
- [x] 4.3 验证后台日报发布后小程序日报列表和详情可读取。
- [x] 4.4 验证日报评论、日报引用发帖、社区审核队列和审核结果回显链路。
- [x] 4.5 修复内容链路中的阻断性空状态、错误态、审核反馈或引用快照问题。

## 5. CPS 与生存账本链路稳定化

- [x] 5.1 验证补给铺公开列表、补给点击、服务端 `sid` 生成和转链结果链路。
- [x] 5.2 验证补给点击响应和小程序页面不暴露 CPS 平台内部字段、佣金、聚推客密钥、后台备注或订单排查字段。
- [x] 5.3 验证模拟或 fixture 订单同步能通过有效 `sid` 匹配用户并写入生存账本。
- [x] 5.4 验证未支付、退款、无效、风控、未知或无法匹配归因的订单不会计入今日摘要和本周报告。
- [x] 5.5 修复补给铺、订单同步或生存账本链路中阻断内测闭环的问题。

## 6. 后台运营链路稳定化

- [x] 6.1 验证后台 token 对工作台、审核队列、社区治理、日报运营和补给铺排查接口的保护。
- [x] 6.2 验证实时待办事件、降级轮询和待办摘要 payload 的敏感字段边界。
- [x] 6.3 验证统一审核队列、社区治理、日报发布、补给铺配置和订单同步排查的加载态、错误态、空状态和操作反馈。
- [x] 6.4 验证编辑中不被实时刷新覆盖、筛选条件保留和局部处理反馈。
- [x] 6.5 修复后台链路中阻断内测闭环的问题。

## 7. 人工内测检查清单

- [x] 7.1 新增内测人工检查清单，按普通用户、后台运营人员和开发配置人员拆分路径。
- [x] 7.2 在清单中标注每个检查项的预期结果、失败记录方式和是否依赖真实外部服务。
- [x] 7.3 在清单中覆盖微信开发者工具首跑、真机 API 地址、后台实时待办、真实补给转链、真实订单回流和真实 AI 审核检查。
- [x] 7.4 明确人工外部服务检查不属于默认确定性总 smoke 的通过前提。

## 8. 验收

- [x] 8.1 运行 `openspec validate stabilize-mvp-end-to-end-beta-readiness --strict`。
- [x] 8.2 运行新增总 smoke 命令。
- [x] 8.3 运行 `pnpm typecheck`。
- [x] 8.4 运行 `pnpm build:api`。
- [x] 8.5 运行 `pnpm build:admin`。
- [x] 8.6 运行 `pnpm build:miniapp`。
