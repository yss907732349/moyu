## 1. 视觉规范和资产矩阵

- [x] 1.1 梳理现有 `IllustrationKeys` 与高频页面实际插画位，补齐 asset key、推荐尺寸、展示比例和安全区说明
- [x] 1.2 在视觉系统文档中记录插画生成提示词、禁用元素、动态 UI 边界和 fallback 规则
- [x] 1.3 明确首页、补给铺、社区、我的页、生存账本、首跑、空状态和错误状态的资产类型映射
- [x] 1.4 检查现有静态图片尺寸和命名，记录需要保留、替换或后续补齐的资产清单

## 2. 视觉系统实现基础

- [x] 2.1 完善小程序端插画资产解析结构，支持按稳定 key 获取图片或 fallback
- [x] 2.2 建立统一视觉状态类型，覆盖加载、空数据、未登录、未建档、配置缺失、网络失败、降级展示、补给不可用和订单未同步
- [x] 2.3 实现或整理可复用状态面板结构，用于展示状态插画、标题、说明和主操作
- [x] 2.4 替换通用 `vs-asset-placeholder` 中会暴露开发态语义的表现，确保正式 UI 不显示占位文字、尺寸或 TODO
- [x] 2.5 补齐语义 icon fallback，避免菜单、板块和入口继续使用完全相同的无语义圆点

## 3. 首页视觉打磨

- [x] 3.1 为首页主卡接入工作场景插画或同风格 fallback，并保证金额、倒计时和设置入口仍由原生 UI 渲染
- [x] 3.2 为隐藏模式接入低调视觉状态，遮挡敏感金额但保留恢复入口
- [x] 3.3 为工作档案未配置状态接入统一配置引导视觉，不使用演示金额或假工作档案
- [x] 3.4 为工作档案同步失败但存在本地快照的状态接入降级提示视觉
- [x] 3.5 为首页日报、补给铺入口和倒计时卡补齐语义图标或 banner fallback

## 4. 补给铺和生存账本视觉打磨

- [x] 4.1 为补给铺三类板块接入语义 icon 或同风格 fallback
- [x] 4.2 优化补给铺主推荐、空补给、板块无商品和补给不可用状态的视觉层级
- [x] 4.3 为补给点击转链失败状态接入统一错误视觉，并隐藏内部排查字段
- [x] 4.4 为生存账本三类摘要接入语义 icon，不暴露 CPS 平台或订单源内部字段
- [x] 4.5 为生存账本空订单、同步中和网络失败状态接入统一状态视觉

## 5. 社区列表和我的页视觉打磨

- [x] 5.1 为社区首页顶部 Banner 接入生产级世界观/漫画 IP 入口视觉，移除无操作价值占位表达
- [x] 5.2 为社区空列表、接口失败、未登录和未建档状态接入统一状态视觉
- [x] 5.3 检查社区帖子缩略图固定尺寸和裁切策略，确保图片不拉高列表卡片
- [x] 5.4 为我的页菜单入口补齐 `我的角色`、`工作设置`、`我的帖子`、`收藏帖子`、`论坛消息` 和 `生存账本` 的语义 icon
- [x] 5.5 为我的页未登录、未建档、资料同步失败和本地快照降级状态接入统一状态视觉
- [x] 5.6 检查阵营头像、徽章和阵营插画在我的页与角色页的显示裁切和安全区

## 6. 首跑引导和状态一致性

- [x] 6.1 将 `logged_out`、`profile_missing`、`work_profile_missing` 和 `ready` 映射到统一视觉状态
- [x] 6.2 确保首页、我的页和相关引导页对同一首跑状态使用一致标题、说明、主操作和插画语义
- [x] 6.3 检查无本地快照且 API 不可用时的错误视觉，避免恢复演示身份、演示金额或演示订单

## 7. 验证和收尾

- [x] 7.1 运行 `pnpm typecheck`
- [x] 7.2 运行 `pnpm build:miniapp`
- [x] 7.3 运行与本 change 相关的验证脚本，包括 `pnpm verify:miniapp-work-profile`、`pnpm verify:miniapp-supply-center`、`pnpm verify:miniapp-community-lite`、`pnpm verify:miniapp-accounting-ledger`、`pnpm verify:miniapp-user-growth-profile` 和 `pnpm verify:mvp-beta-readiness`
- [x] 7.4 手工检查首页、补给铺、社区列表、我的页、生存账本和首跑状态在小屏下无文字重叠、无开发态占位、无敏感字段暴露
- [x] 7.5 更新内测或设计说明，记录插画资产清单、生成提示词、尺寸、安全区和后续补图边界
