## 1. 共享节假日模型

- [x] 1.1 在 `packages/shared` 新增节假日缓存类型，覆盖节假日名称、年份、开始日期、结束日期、数据状态、同步来源和权威核验元数据。
- [x] 1.2 在 `packages/shared` 新增节假日缓存数据入口，支持保存当前年和下一年的同步结果或待发布状态。
- [x] 1.3 实现共享查询函数，根据当前日期返回下一个法定节假日、进行中节假日或无可用数据状态。
- [x] 1.4 从 `packages/shared/src/index.ts` 导出节假日类型、缓存数据和查询函数。

## 2. 同步与验证脚本

- [x] 2.1 新增节假日同步脚本，从候选免费接口拉取中国大陆节假日数据并归一化为共享缓存结构。
- [x] 2.2 同步脚本在接口失败、字段异常或数据不完整时报告错误，并保留现有可用缓存。
- [x] 2.3 新增节假日验证脚本，检查日期格式、区间顺序、年份覆盖、来源元数据和权威核验状态。
- [x] 2.4 验证脚本支持未来年份权威通知未发布的显式状态，并禁止预测日期被标记为已核验。
- [x] 2.5 在根 `package.json` 增加 `sync:holidays` 和 `verify:holidays` 命令。

## 3. 首页倒计时接入

- [x] 3.1 小程序首页从共享节假日查询函数获取下一个法定节假日倒计时数据。
- [x] 3.2 首页倒计时卡展示节假日名称、剩余时间和数据不可用时的待发布、待同步或待接入占位。
- [x] 3.3 移除或替换页面内任何过期或预测的节假日硬编码日期。
- [x] 3.4 确认节假日倒计时不影响今日已摸金额、工作状态、每秒收益和工作进度计算。

## 4. 边界与验收

- [x] 4.1 确认小程序首页运行时不直接请求第三方免费节假日接口。
- [x] 4.2 确认第一版不实现调休上班日、请假、大小周、多班次轮班或复杂排班。
- [x] 4.3 运行 `pnpm verify:holidays` 验证节假日缓存数据。
- [x] 4.4 运行 `pnpm verify:miniapp-work-profile` 验证首页工作价值相关行为未回退。
- [x] 4.5 运行 `pnpm typecheck` 验证 workspace 类型检查通过。
