## 1. 共享契约与配置

- [x] 1.1 在 `packages/shared` 新增供应商无关内容安全结果、来源、风险标签、人工复核原因、作者自见状态和图片审核状态契约。
- [x] 1.2 保留现有 AI 审核 trace 兼容字段，并新增内容安全归一化摘要字段，确保后台和旧验证不因字段重命名中断。
- [x] 1.3 增加微信内容安全 label、suggest 和错误结果到项目统一风险标签与业务决策的映射函数。
- [x] 1.4 扩展 `.env.example` 和相关配置读取，覆盖微信内容安全启用、mock、供应商错误模拟、图片回调和超时配置。
- [x] 1.5 评估并补充 Prisma schema 或 JSON trace 结构，用于保存文本审核摘要、图片审核任务标识、图片审核状态和作者自见所需字段。

## 2. 微信内容安全服务

- [x] 2.1 新增服务端微信 access token 客户端，支持缓存、过期刷新、错误归一化和敏感日志过滤。
- [x] 2.2 实现微信文本内容安全调用，支持社区帖子、评论、回复、日报评论、大陆新闻评论和公开资料字段场景。
- [x] 2.3 实现微信图片内容安全任务提交，校验图片 URL 可下载、图片类型、大小上限和任务标识保存。
- [x] 2.4 新增微信图片内容安全异步回调接口，完成签名或来源校验、`trace_id` 匹配、幂等更新和迟到回调保护。
- [x] 2.5 实现内容安全 mock、供应商错误、超时、无有效 openid 和响应格式异常的统一降级。
- [x] 2.6 为内容安全服务新增单元验证，覆盖 `pass`、`risky`、`review`、错误和敏感字段过滤。

## 3. 业务入口接入

- [x] 3.1 将社区帖子文本接入本地规则和微信内容安全链路，支持通过后自动公开、违规自动驳回、无法确认进入人工复核。
- [x] 3.2 将社区评论和回复接入“本地快速通道 + 微信升级 + 人工兜底”三通道分流，并保留可信短文本快速通过。
- [x] 3.3 将社区图片资产接入微信图片异步审核，保证所有图片通过前帖子不对其他用户公开。
- [x] 3.4 将日报文章评论接入统一内容安全链路，并支持提交者本人查看待审核评论。
- [x] 3.5 将大陆新闻文章评论接入统一内容安全链路，并支持提交者本人查看待审核评论。
- [x] 3.6 将昵称等会进入公开作者快照的本人资料字段接入内容安全审核，违规或无法确认时阻止保存或进入明确处理边界。
- [x] 3.7 增加微信 openid 获取和近两小时访问要求的降级处理，确保无法满足条件时进入人工复核而不是假装通过。

## 4. 作者自见与小程序体验

- [x] 4.1 扩展社区帖子详情、我的帖子和必要列表响应，使作者能看到本人 `pending` 帖子且其他用户不可见。
- [x] 4.2 扩展社区评论和回复响应，保持待审核内容仅作者本人可见，并输出前端可识别的状态。
- [x] 4.3 更新小程序社区发帖、帖子详情、我的论坛、评论和回复 UI，使用轻量“审核中/仅自己可见/通过后公开”文案。
- [x] 4.4 更新日报和大陆新闻评论区 UI，使提交者可看到自己的待审核评论，其他用户只看到公开评论。
- [x] 4.5 检查所有小程序文案，确保未公开内容不被表达为已对所有用户公开。

## 5. 后台运营与治理

- [x] 5.1 扩展后台统一审核队列，展示本地规则、微信文本、微信图片、mock、降级和人工复核的归一化摘要。
- [x] 5.2 扩展后台工作台统计，区分自动通过、自动驳回、微信待回调、微信无法确认和人工复核数量。
- [x] 5.3 扩展审核详情和社区治理详情，展示图片审核状态、脱敏任务标识、风险标签、原因和可用操作。
- [x] 5.4 更新后台筛选能力，支持按内容安全来源、风险标签、人工复核原因和图片审核状态筛选。
- [x] 5.5 复查后台响应敏感字段过滤，确保不返回微信密钥、`access_token`、`sessionKey`、完整 openid、完整 unionid 或供应商完整原始响应。

## 6. 验证、文档与验收

- [x] 6.1 新增或扩展 `pnpm verify:ai-content-moderation`，覆盖本地规则、微信文本、mock、供应商错误和敏感字段边界。
- [x] 6.2 新增或扩展社区验证，覆盖帖子自动公开、作者自见、评论快速通过、微信复核、自动驳回和图片异步审核。
- [x] 6.3 新增或扩展日报和大陆新闻评论验证，覆盖作者自见、供应商通过、供应商驳回和人工复核。
- [x] 6.4 新增或扩展后台运营验证，覆盖内容安全摘要、筛选、图片状态、工作台统计和敏感字段黑名单。
- [x] 6.5 更新 `docs/mvp-beta-readiness.md` 和 `docs/mvp-beta-manual-checklist.md`，补充微信内容安全配置、mock、图片回调和作者自见检查。
- [x] 6.6 运行 `openspec validate integrate-wechat-content-security-moderation --strict`。
- [x] 6.7 运行 `pnpm typecheck`、`pnpm lint` 和相关 `pnpm verify:*` 命令。
- [x] 6.8 记录真实微信内容安全接口配置和回调域名的人工内测注意事项，不提交真实密钥或真实用户身份字段。
