## 1. 共享契约

- [x] 1.1 重构 `packages/shared` 中的账单契约，将旧收入/支出账单类型替换或隔离为 CPS 生存账单类型。
- [x] 1.2 定义三类稳定生存分类 key、展示名、图标 key 和排序：`隐者食堂`、`下午续命`、`通勤`。
- [x] 1.3 定义 CPS 来源订单、来源平台、来源状态、生存账单快照、今日摘要和本周生存报告响应契约。
- [x] 1.4 实现下午续命关键词归类规则，并覆盖咖啡、奶茶、茶饮、甜品、蛋糕和下午茶等语义。
- [x] 1.5 实现今日生存消耗和本周生存报告统计函数，确保取消、退款或无效订单不计入有效消耗。

## 2. API 与持久化

- [x] 2.1 调整 Prisma 模型或新增模型，保存 CPS 原始订单来源字段和生存账单展示字段。
- [x] 2.2 实现 CPS 订单导入/同步服务，按来源平台和来源订单 ID 去重更新生存账单。
- [x] 2.3 实现订单状态同步逻辑，取消、退款或无效订单应从有效消耗统计中排除。
- [x] 2.4 改造 `/me/accounting-ledger` 相关本人接口，提供今日摘要、订单详情和本周生存报告读取能力。
- [x] 2.5 下线、禁用或隔离旧手动创建、更新、作废账单接口，避免旧手动账单混入新统计。
- [x] 2.6 确保普通用户 API 响应不返回用于展示的平台名称，服务端仅保留排查和去重所需来源字段。

## 3. 小程序体验

- [x] 3.1 改造首页底部 tab 上方区域，展示 `今日生存消耗`、三类金额和 `查看详情` 入口。
- [x] 3.2 将首页账单入口从旧 `记一笔` 流程改为生存账单详情页。
- [x] 3.3 替换或重构账单页面，展示订单详情和本周生存报告。
- [x] 3.4 从普通用户 UI 中移除美团、饿了么、滴滴等 CPS 平台名称展示。
- [x] 3.5 移除或重定向旧手动记账记录页，避免第一版主流程继续出现专业记账表单。
- [x] 3.6 更新 feature registry 默认账单入口路由和提示文案。

## 4. 旧数据与边界

- [x] 4.1 明确旧手动账单数据隔离策略，新生存账单统计不得读取旧收入/支出账单。
- [x] 4.2 为通勤距离缺失场景提供展示降级，不能凭空生成真实公里数。
- [x] 4.3 为 `击败了 X% 的键隐者` 字段提供预留或占位策略，并明确不表达真实排行。
- [x] 4.4 保持账单明细、消费来源、分类统计和本周报告仅本人可见，不接入社区或公开资料。

## 5. 验证

- [x] 5.1 新增或更新共享契约验证脚本，覆盖三类分类、关键词归类、去重和统计口径。
- [x] 5.2 新增或更新 API 验证脚本，覆盖 CPS 订单导入、状态更新、本人上下文和平台名称隐藏。
- [x] 5.3 新增或更新小程序验证脚本，覆盖首页今日生存消耗、详情页、本周报告和旧 `记一笔` 入口移除。
- [x] 5.4 运行 `openspec validate redefine-accounting-ledger-as-cps-survival-ledger --strict`。
- [x] 5.5 修改实现后运行 `pnpm format`、`pnpm format:check`、`pnpm lint`、`pnpm typecheck`、`pnpm build:api` 和 `pnpm build:miniapp`。
