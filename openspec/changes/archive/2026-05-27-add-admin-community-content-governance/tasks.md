## 1. 共享契约

- [x] 1.1 在 `packages/shared` 新增后台社区内容治理契约，覆盖帖子总览、搜索请求、搜索响应、帖子治理详情、评论/回复治理项和分页信息。
- [x] 1.2 新增社区治理动作枚举，覆盖隐藏、移除、禁言、限制、封禁、解封和查看详情等后台动作。
- [x] 1.3 新增社区用户治理状态枚举，覆盖 `normal`、`limited`、`muted` 和 `banned`。
- [x] 1.4 新增治理操作审计契约，覆盖目标类型、目标 ID、目标作者 ID、旧状态、新状态、原因、备注、操作人和创建时间。
- [x] 1.5 扩展后台敏感字段黑名单，补充微信 `sessionKey`、明文 IP、IP 相关明文字段和社区治理响应禁止字段。
- [x] 1.6 更新共享导出入口，确保 API、后台和验证脚本可复用社区治理契约。

## 2. 数据模型与迁移

- [x] 2.1 扩展 Prisma schema，增加社区用户治理状态或治理记录模型，保存用户 ID、状态、期限、原因、备注、操作人、创建时间和解除信息。
- [x] 2.2 增加社区治理操作审计模型，保存内容治理和用户治理操作历史。
- [x] 2.3 为帖子、评论和回复补充移除语义所需字段或状态，并确保公开查询可过滤。
- [x] 2.4 为社区内容搜索和后台筛选补充必要索引，例如状态、作者、创建时间和分区组合索引。
- [x] 2.5 如实现 IP 风险预留，仅保存脱敏值、哈希摘要或风险标签，不保存明文 IP。
- [x] 2.6 生成 Prisma 迁移并确保现有数据默认兼容 `normal` 用户治理状态。

## 3. 后台社区治理 API

- [x] 3.1 新增后台社区治理控制器和服务，所有接口校验 `x-admin-token`。
- [x] 3.2 实现 `GET /admin/community-governance/posts`，支持全量帖子总览、状态筛选、分区筛选、作者筛选、时间排序和分页。
- [x] 3.3 实现后台关键词搜索，支持标题、正文、帖子 ID、作者用户 ID 和作者公开昵称定位。
- [x] 3.4 实现 `GET /admin/community-governance/posts/:postId`，返回帖子全文、评论、回复、风险摘要、举报摘要、作者治理状态和治理历史。
- [x] 3.5 实现帖子治理操作接口，支持隐藏和移除，并写入治理审计。
- [x] 3.6 实现评论和回复治理操作接口，支持隐藏和移除，并写入治理审计。
- [x] 3.7 实现作者治理接口，支持限制、禁言、封禁、解除限制和读取作者治理详情。
- [x] 3.8 确保后台治理响应不包含薪资、工作档案、生存账单、CPS 来源、微信身份、登录态、明文 IP、AI 原始 prompt、供应商原始响应或密钥字段。

## 4. 社区写操作拦截

- [x] 4.1 在社区服务层增加统一用户治理状态检查函数。
- [x] 4.2 在发帖入口拦截 `muted` 和 `banned` 用户，并对 `limited` 用户采用更严格审核策略。
- [x] 4.3 在评论入口拦截 `muted` 和 `banned` 用户，并取消 `limited` 用户的低风险快速公开路径。
- [x] 4.4 在回复入口拦截 `muted` 和 `banned` 用户，并取消 `limited` 用户的低风险快速公开路径。
- [x] 4.5 在社区媒体上传入口拦截 `muted` 和 `banned` 用户。
- [x] 4.6 处理限制到期逻辑，过期限制不得继续阻止用户社区写操作。

## 5. 管理后台界面

- [x] 5.1 在后台运营中心新增社区内容治理入口，并与统一审核队列入口区分。
- [x] 5.2 新增社区帖子总览视图，展示所有状态帖子，默认按创建时间倒序排列。
- [x] 5.3 新增搜索和筛选控件，支持关键词、状态、分区、作者、风险和时间范围筛选。
- [x] 5.4 新增帖子治理详情视图，展示帖子全文、作者信息、状态、风险摘要、举报摘要、评论和回复上下文。
- [x] 5.5 在帖子、评论和回复上提供隐藏、移除和查看上下文操作，并要求填写原因或备注。
- [x] 5.6 在作者区域提供限制、禁言、封禁和解除限制操作，支持期限、原因和后台备注。
- [x] 5.7 展示内容治理和用户治理操作历史，避免运营人员重复处置或误判状态。
- [x] 5.8 确保后台界面不展示明文 IP、微信身份、登录态、工作档案、生存账单或其他敏感字段。

## 6. 验证脚本

- [x] 6.1 新增或扩展 `scripts/verify-admin-community-governance` 验证脚本，检查后台社区治理契约和入口。
- [x] 6.2 验证帖子总览支持全状态、倒序排列、分页和状态筛选。
- [x] 6.3 验证关键词搜索支持标题、正文、帖子 ID、作者用户 ID 和作者昵称定位。
- [x] 6.4 验证帖子治理详情返回评论、回复、举报摘要、作者治理状态和治理历史。
- [x] 6.5 验证隐藏和移除帖子、评论、回复后，公开侧不再返回对应内容。
- [x] 6.6 验证禁言和封禁用户不能发帖、评论、回复或上传社区图片。
- [x] 6.7 验证解除限制或限制过期后，用户可重新进入正常社区写操作流程。
- [x] 6.8 验证社区治理响应不包含敏感字段黑名单。
- [x] 6.9 在根 `package.json` 新增后台社区治理验证命令，并按需纳入后台运营验证。

## 7. 验收

- [x] 7.1 运行 `openspec validate add-admin-community-content-governance --strict`。
- [x] 7.2 运行 `pnpm format`。
- [x] 7.3 运行 `pnpm format:check`。
- [x] 7.4 运行 `pnpm lint`。
- [x] 7.5 运行 `pnpm typecheck`。
- [x] 7.6 运行 `pnpm verify:community-lite`、`pnpm verify:community-lite-api` 和 `pnpm verify:admin-community-lite`。
- [x] 7.7 运行 `pnpm verify:admin-operations`。
- [x] 7.8 运行新增后台社区治理验证命令。
- [x] 7.9 运行 `pnpm build:api` 和 `pnpm build:admin`。
