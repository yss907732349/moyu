## 1. 共享契约与事件模型

- [x] 1.1 在 `packages/shared` 新增后台实时事件类型，覆盖事件 ID、事件类型、来源、目标类型、目标 ID、创建时间、待办计数和队列变化摘要。
- [x] 1.2 新增或扩展审核队列项前端处理态类型，覆盖 `idle`、`processing`、`succeeded`、`failed` 和 `stale`。
- [x] 1.3 新增后台连接状态、模块加载状态、操作反馈状态和错误原因归一化类型。
- [x] 1.4 确认共享契约不包含薪资、工作档案、微信身份、用户登录态、AI 原始 prompt、供应商原始响应、聚推客密钥或完整订单同步原始响应字段。

## 2. API 实时事件流与降级事实源

- [x] 2.1 新增 `GET /admin/operations/events` 或等价后台事件流接口，使用 `x-admin-token` 校验并返回 `text/event-stream`。
- [x] 2.2 实现后台运营事件发布机制，在新审核、新举报、日报评论复核、工作台计数变化和审核状态变化时生成最小事件。
- [x] 2.3 为事件 payload 建立独立 DTO 和字段过滤，避免复用包含正文详情、内部排查字段或供应商响应的对象。
- [x] 2.4 新增或复用轻量待办摘要读取能力，供前端在 SSE 断开时进行低频轮询。
- [x] 2.5 处理事件流心跳、连接关闭、服务端错误和无效令牌响应。
- [x] 2.6 为审核、举报、日报评论和发布操作补充状态已变化、重复处理、目标不存在、令牌失效和校验失败的明确错误响应。

## 3. 后台实时连接与新待办提示

- [x] 3.1 在 `apps/admin` 抽取后台请求客户端，统一携带 `x-admin-token`、错误归一化和令牌失效提示。
- [x] 3.2 使用 `fetch` 流式读取后台 SSE，解析事件 ID、事件类型和待办摘要。
- [x] 3.3 实现连接状态机，覆盖连接中、实时连接中、降级轮询、离线和恢复状态。
- [x] 3.4 在 SSE 失败、超时或断开时启动低频轮询，并在 SSE 恢复后停止轮询。
- [x] 3.5 在顶部展示“有 X 条新待办”或等价提示，点击后刷新当前工作台、队列或模块事实源。
- [x] 3.6 为审核备注、治理原因、日报正文、补给项表单和详情处理面板维护脏状态，阻止实时事件自动覆盖当前输入。

## 4. 审核队列体验优化

- [x] 4.1 重排统一审核队列表格字段，使来源、对象类型、状态、风险标签、审核来源、用户风险原因、摘要、作者和时间更易扫读。
- [x] 4.2 区分帖子、评论、回复、举报和日报评论的类型标签与可用操作。
- [x] 4.3 调整通过、驳回、隐藏、查看详情、处理举报等按钮层级，确保高频安全操作和高风险操作有清晰区分。
- [x] 4.4 执行队列操作时将当前行置为 `processing`，并阻止重复提交。
- [x] 4.5 操作成功后按当前筛选条件局部移出队列或更新该行状态、可用操作和反馈。
- [x] 4.6 操作失败时展示令牌失效、网络失败、状态不允许、目标不存在、校验失败或状态已变化的明确原因。
- [x] 4.7 从队列详情返回列表时保留筛选条件、当前连接状态和新待办提示。

## 5. 工作台与后台模块一致化

- [x] 5.1 优化工作台统计卡和最近待办布局，展示实时连接状态、降级轮询状态和新待办入口。
- [x] 5.2 统一筛选栏、表格、详情面板、加载态、错误态、空状态和无结果状态的组件或局部结构。
- [x] 5.3 社区治理列表和详情接入统一操作反馈、状态已变化提示、筛选保留和敏感字段边界。
- [x] 5.4 日报运营模块接入统一加载、错误、刷新和编辑中不覆盖规则。
- [x] 5.5 大陆新闻管理模块接入统一筛选保留、操作反馈和错误态。
- [x] 5.6 补给铺配置和同步排查模块接入统一加载、错误、刷新和敏感字段提示边界。

## 6. 验证脚本与质量检查

- [x] 6.1 扩展 `pnpm verify:admin-operations`，覆盖实时事件接口的 `x-admin-token` 校验。
- [x] 6.2 扩展 `pnpm verify:admin-operations`，检查实时事件 payload 不包含敏感字段黑名单。
- [x] 6.3 扩展 `pnpm verify:admin-operations`，覆盖降级轮询、新待办提示、编辑中不自动覆盖和队列操作局部反馈。
- [x] 6.4 扩展 `pnpm verify:admin-community-governance`，覆盖社区治理筛选保留、操作反馈、状态已变化提示和敏感字段边界。
- [x] 6.5 必要时扩展 `pnpm verify:admin-daily-content-feed`，覆盖日报运营编辑中不被刷新覆盖和统一反馈。
- [x] 6.6 必要时扩展 `pnpm verify:admin-supply-center`，覆盖补给铺后台排查响应不暴露密钥和完整原始响应。
- [x] 6.7 运行 `openspec validate improve-admin-realtime-review-and-operations-ux --strict`。
- [x] 6.8 运行 `pnpm typecheck`。
- [x] 6.9 运行 `pnpm build:admin`。
- [x] 6.10 运行 `pnpm build:api`。
