## 1. 数据模型与共享契约

- [x] 1.1 确认 `packages/shared` 中现有工作档案类型、校验、快照和响应契约可直接供 API 与小程序端复用。
- [x] 1.2 在 Prisma schema 中新增工作档案模型，按 `userId` 保证每个用户只有一份当前有效工作档案。
- [x] 1.3 为工作档案模型设计字段映射，覆盖薪资、工作时间、休息段、工作日、发薪日、隐藏模式、创建时间和更新时间。
- [x] 1.4 生成并提交工作档案数据库迁移和 Prisma Client 更新。

## 2. 后端工作档案 API

- [x] 2.1 新增临时本人上下文实现，并明确标记为真实微信登录前的开发占位。
- [x] 2.2 新增工作档案服务，负责从 Prisma 记录转换为共享包 `WorkProfile`、`WorkProfileSnapshot` 和响应结构。
- [x] 2.3 实现 `GET /me/work-profile`，返回本人工作档案、配置状态和可用快照。
- [x] 2.4 实现 `PUT /me/work-profile`，复用共享校验规则保存或覆盖本人工作档案。
- [x] 2.5 将共享校验错误转换为客户端错误响应，并保留可用于表单字段反馈的信息。
- [x] 2.6 确保后端不会在功能入口或其他公开响应中返回工作档案敏感字段。

## 3. 小程序工作档案服务

- [x] 3.1 新增小程序端 `work-profile` 服务，封装工作档案读取、保存、超时和网络错误处理。
- [x] 3.2 为工作档案 API 响应增加运行时结构校验，避免页面消费非法响应。
- [x] 3.3 将服务端校验错误归一为设置页可展示的字段错误或全局错误。
- [x] 3.4 保持 API base URL 和请求风格与现有功能入口服务一致。

## 4. 小程序设置页

- [x] 4.1 新增 `pages/work-profile/settings` 页面并注册到 `pages.json`。
- [x] 4.2 构建暗黑忍者 RPG 风格的工作档案设置页布局，沿用现有视觉 token 和面板样式。
- [x] 4.3 实现月薪金额、上班开始时间、上班结束时间、休息段、工作日规则、发薪日和隐藏模式表单状态。
- [x] 4.4 页面加载时读取本人工作档案；已配置时填充表单，未配置时展示默认值或引导态。
- [x] 4.5 保存前执行基础前端校验，并在保存失败时展示字段级错误。
- [x] 4.6 保存成功后展示明确反馈，并保留保存后的 `WorkProfileSnapshot` 供后续首页消费。

## 5. 功能入口与导航

- [x] 5.1 更新默认功能注册表，将 `salary_work_time_settings` 的公开路由指向工作档案设置页。
- [x] 5.2 确认首页功能入口点击工作流可以从首页进入工作档案设置页。
- [x] 5.3 确认非开放入口仍遵守现有导航拦截规则，不因本 change 产生回归。

## 6. 验证

- [x] 6.1 增加或更新共享工作档案验证脚本，覆盖本 change 涉及的响应契约和快照兼容性。
- [x] 6.2 增加 API 验证，覆盖读取未配置状态、首次保存、覆盖保存和非法请求拒绝。
- [x] 6.3 增加小程序端服务验证，覆盖成功响应、非法响应、网络失败和校验错误归一。
- [x] 6.4 增加功能入口验证，确认 `salary_work_time_settings` 路由指向设置页。
- [x] 6.5 运行 `pnpm typecheck`、相关验证脚本和必要构建命令，确认 change 可实现并无类型回归。
