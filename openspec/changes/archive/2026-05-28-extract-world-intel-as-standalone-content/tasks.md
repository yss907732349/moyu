## 1. 共享契约与规格边界

- [x] 1.1 在 `packages/shared` 新增大陆新闻文章、状态、来源、公开列表、公开详情和后台编辑请求/响应类型。
- [x] 1.2 增加大陆新闻来源校验函数，复用外部采编来源完整性和原创来源 URL 格式规则。
- [x] 1.3 调整日报共享契约，移除大陆新闻作为日报文章栏目保存、发布和 10 条上限校验对象的语义。
- [x] 1.4 更新日报摘要或栏目入口契约，使大陆新闻入口可指向独立内容库并展示独立计数或近期摘要。

## 2. 数据模型与历史迁移

- [x] 2.1 在 Prisma schema 中新增大陆新闻文章持久化模型，包含公开字段、来源字段、状态、发布时间、更新时间和旧内容追溯字段。
- [x] 2.2 创建数据库迁移，补充大陆新闻文章索引，覆盖状态、发布时间、更新时间和旧内容 ID 去重。
- [x] 2.3 实现历史 `world_intel` 已发布内容迁移逻辑或脚本，保留标题、摘要、正文、来源、配图和发布时间。
- [x] 2.4 确保迁移重复运行不会创建重复文章，并在开发环境提供兼容读取兜底。

## 3. 大陆新闻 API

- [x] 3.1 新增公开大陆新闻列表接口，支持分页、服务端最大 `pageSize` 和只返回已发布文章。
- [x] 3.2 新增公开大陆新闻详情接口，过滤草稿、隐藏、下线和不存在文章。
- [x] 3.3 新增后台大陆新闻列表和详情接口，校验 `x-admin-token` 并支持状态/关键词筛选。
- [x] 3.4 新增后台单篇创建、批量创建、编辑保存、发布、下线或隐藏接口。
- [x] 3.5 接入来源完整性校验，确保外部采编发布前必须补齐来源 URL、来源标题和来源站点。
- [x] 3.6 确保大陆新闻公开和后台响应不暴露敏感字段、AI 原始 prompt、供应商响应或密钥配置。

## 4. 日报 API 调整

- [x] 4.1 调整日报板块级保存接口，拒绝通过日报接口保存 `world_intel` 并提示使用大陆新闻独立接口。
- [x] 4.2 调整日报板块级发布接口，拒绝通过日报接口发布 `world_intel`，且不得影响已发布大陆新闻。
- [x] 4.3 调整日报公开摘要组合逻辑，使大陆新闻入口从独立内容库读取计数或近期摘要。
- [x] 4.4 移除公开大陆新闻列表从日报 section `items.slice(0, 10)` 读取的依赖。
- [x] 4.5 保持 `daily_reflection` 和 `absurd_casefile` 的既有公开、校验、预览和发布行为可用。

## 5. 后台页面

- [x] 5.1 在管理后台新增大陆新闻独立导航入口和管理页面。
- [x] 5.2 实现大陆新闻后台列表，展示标题、状态、来源、发布时间、更新时间和可用操作。
- [x] 5.3 实现大陆新闻单篇编辑表单，覆盖标题、摘要、正文、来源、配图、引用设置和发布状态操作。
- [x] 5.4 实现大陆新闻批量新增体验，允许一次提交多篇文章且不使用 10 条业务上限。
- [x] 5.5 从日报运营页面移除大陆新闻编辑工作区和日报板块新建选项。
- [x] 5.6 在日报运营页面保留跳转大陆新闻管理页的提示或入口，避免运营找不到原功能。

## 6. 小程序公开体验

- [x] 6.1 调整小程序日报首页大陆新闻入口，使其跳转到独立大陆新闻列表。
- [x] 6.2 新增或调整大陆新闻列表页，支持历史文章分页、加载更多、空状态和错误状态。
- [x] 6.3 调整大陆新闻详情页数据来源，展示公开来源、正文、配图、发布时间和更新时间。
- [x] 6.4 确保小程序不使用本地演示文章填充真实大陆新闻列表或详情。
- [x] 6.5 保持离谱卷宗和今日参悟的原日报体验不被破坏。

## 7. 验证与回归

- [x] 7.1 更新 `pnpm verify:daily-content-feed-api`，覆盖大陆新闻独立公开列表、详情、分页、历史累积和日报接口拒绝 `world_intel` 保存/发布。
- [x] 7.2 更新或新增后台大陆新闻验证，覆盖独立入口、批量新增、编辑已发布、发布、下线和 `x-admin-token`。
- [x] 7.3 更新 `pnpm verify:admin-daily-content-feed`，确认日报运营不再直接编辑大陆新闻。
- [x] 7.4 更新 `pnpm verify:miniapp-daily-content-feed`，覆盖日报入口跳转大陆新闻列表和历史分页。
- [x] 7.5 运行 `pnpm typecheck` 并修复类型错误。
- [x] 7.6 运行 `pnpm verify:daily-content-feed-api`。
- [x] 7.7 运行 `pnpm verify:admin-daily-content-feed` 和后台大陆新闻相关验证。
- [x] 7.8 运行 `pnpm verify:miniapp-daily-content-feed`。

## 8. 验收

- [x] 8.1 手动确认连续发布多批大陆新闻后，旧文章仍可在小程序历史列表中看到。
- [x] 8.2 手动确认单次新增超过 10 篇大陆新闻不会被业务规则阻止。
- [x] 8.3 手动确认编辑已发布大陆新闻后，公开详情展示最新内容并保留发布时间/更新时间边界。
- [x] 8.4 手动确认日报运营页不能再保存或发布大陆新闻板块。
- [x] 8.5 手动确认离谱卷宗仍受 10 篇上限约束，今日参悟仍最多 1 条。
