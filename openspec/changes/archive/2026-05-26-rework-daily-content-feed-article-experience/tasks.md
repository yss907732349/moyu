## 1. 共享契约与数据结构

- [x] 1.1 更新 `packages/shared` 日报契约，新增每日参悟、栏目入口、日报文章、文章评论、文章引用快照和公开响应类型
- [x] 1.2 更新日报校验函数，覆盖栏目 key、每栏目最多 10 篇文章、每日参悟短文本、公开字段过滤和来源边界
- [x] 1.3 扩展社区引用共享契约，支持 `daily_reflection` 和 `daily_article` 两类引用来源
- [x] 1.4 调整默认 feature registry，确认 `daily_content_feed` 入口指向隐者日报首页且不为栏目或文章单独创建 feature key

## 2. API 与持久化

- [x] 2.1 调整日报持久化模型或内存数据模型，支持每日参悟、栏目元数据、栏目文章、文章点赞和文章评论
- [x] 2.2 实现日报摘要接口，返回业务日期、标题、首页摘要、每日参悟和两个栏目入口摘要，不返回栏目文章正文
- [x] 2.3 实现栏目文章列表接口，每个栏目最多返回 10 篇已发布文章和公开展示字段
- [x] 2.4 实现日报文章详情接口，返回正文、来源说明、互动统计、当前用户点赞状态和公开评论列表
- [x] 2.5 实现日报文章点赞、取消点赞、评论提交和评论审核状态过滤
- [x] 2.6 扩展社区发帖接口，支持从每日参悟或日报文章创建带引用快照的待审核帖子
- [x] 2.7 更新 DeepSeek 草稿生成结构，输出每日参悟、两个栏目入口元数据和每栏目最多 10 篇文章
- [x] 2.8 保留 `REVIEW_BYPASS_FOR_TESTING` 测试绕审开关，但正式默认仍保持帖子、评论和日报内容审核后公开

## 3. 管理后台

- [x] 3.1 更新日报运营页面，支持编辑每日参悟、栏目简介、插画 key 和每篇文章内容
- [x] 3.2 在后台展示文章来源、AI 生成元数据和审核状态，避免将内部字段暴露到普通用户接口
- [x] 3.3 支持日报文章评论的最小审核操作，包含查看待审核评论、通过、驳回或隐藏
- [x] 3.4 确保未授权后台用户无法生成、编辑、审核、发布、归档日报或审核日报评论

## 4. 小程序体验

- [x] 4.1 更新日报客户端服务，提供摘要、栏目列表、文章详情、点赞、取消点赞、评论和引用发帖方法
- [x] 4.2 重做隐者日报首页，展示今日参悟和两个栏目入口卡，不直接铺出栏目文章正文
- [x] 4.3 新增日报栏目列表页，展示最多 10 篇文章卡片并支持进入详情
- [x] 4.4 新增日报文章详情页，展示正文、来源、点赞、评论列表、评论输入和转发引用按钮
- [x] 4.5 更新社区发帖页，识别每日参悟和日报文章两类引用快照并生成默认引用提示
- [x] 4.6 确保未登录或未建档用户执行点赞、评论或引用发帖时跳转“我的”页完成身份门槛

## 5. 验证与验收

- [x] 5.1 更新 `pnpm verify:daily-content-feed`，验证文章化共享契约、栏目数量、公开字段过滤和来源边界
- [x] 5.2 更新 `pnpm verify:daily-content-feed-api`，验证摘要、栏目列表、文章详情、点赞、评论审核和 AI 草稿失败边界
- [x] 5.3 更新 `pnpm verify:miniapp-daily-content-feed`，验证日报首页、栏目列表、文章详情、评论和引用发帖入口
- [x] 5.4 扩展 `pnpm verify:community-lite`，验证每日参悟和日报文章引用发帖进入审核且不暴露内部字段
- [x] 5.5 运行 `openspec validate rework-daily-content-feed-article-experience --strict`
- [x] 5.6 运行 `pnpm format` 和 `pnpm format:check`
- [x] 5.7 运行 `pnpm lint`、`pnpm typecheck`、`pnpm build:api` 和 `pnpm build:miniapp`
