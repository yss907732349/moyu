## 1. 共享契约与数据模型

- [x] 1.1 扩展 `packages/shared` 补给铺契约，增加活动分组、用户可见标签、优先级、推荐时段、工作日规则、归因窗口、转链有效期、备用链接策略和普通用户预览响应。
- [x] 1.2 扩展 `packages/shared` CPS 排查契约，增加转链尝试状态、归因失败原因、订单异常类型、链路详情、异常池和经营指标响应类型。
- [x] 1.3 扩展生存账本共享契约，明确 CPS 来源订单幂等键、状态历史、退款回滚、分类异常和不计入统计状态。
- [x] 1.4 扩展 Prisma schema 或等价持久化结构，支持补给活动运营字段、归因 token 摘要、转链尝试、订单状态历史、异常原因和指标聚合所需字段。
- [x] 1.5 编写数据兼容策略，为既有补给项补齐默认分组、标签、优先级和展示规则，为既有点击记录生成 `sidDigest` 与 `sidMasked`。

## 2. 补给铺 API 与归因链路

- [x] 2.1 扩展公开补给铺列表 API，返回今日补给面板、主推荐、三类板块、用户可见标签、可点击状态和同步提示。
- [x] 2.2 实现补给活动排序规则，按有效期、工作日规则、推荐时段、主推荐、分组、优先级、排序值和稳定 ID 生成公开展示顺序。
- [x] 2.3 重构补给点击逻辑，记录每次点击尝试，并按配置生成或复用归因 token 和可用转链目标。
- [x] 2.4 实现 `sid` 原文、`sidDigest`、`sidMasked` 的生命周期边界，确保小程序和后台默认响应不返回完整原始 `sid`。
- [x] 2.5 增加归因窗口、转链有效期、重复点击去重窗口和跨活动匹配校验。
- [x] 2.6 增加转链尝试记录，保存成功、失败、备用链接使用、跳转目标类型、失败原因和目标过期时间。
- [x] 2.7 实现受控备用链接策略，区分可归因备用链接、不可归因备用链接和无备用链接不可用提示。

## 3. CPS 订单同步与生存账本入账

- [x] 3.1 重构聚推客订单同步为 `sourceProvider + sourceOrderId` 幂等更新，确保重复同步不重复入账。
- [x] 3.2 增加订单状态历史或等价审计信息，区分首次同步、重复同步、状态升级、退款回滚和异常排除。
- [x] 3.3 完善订单状态映射，未支付、未知、风控、无效、退款、取消和结算失败状态不得计入有效统计。
- [x] 3.4 实现已入账订单退款、取消、无效或风控后的账本回滚，首页摘要和本周报告随之重新计算。
- [x] 3.5 实现无归因、`sid` 未匹配、归因过期、活动不匹配、分类异常和入账失败订单的异常池记录。
- [x] 3.6 完善 CPS 订单分类优先级，优先使用补给项默认分类，再使用活动、品牌、标题和来源标签兜底。
- [x] 3.7 确保本人账本 API 继续只返回分类、金额、展示标题、发生时间和展示状态，不暴露 CPS 内部字段。

## 4. 小程序补给铺体验

- [x] 4.1 将补给铺页面重构为今日补给面板，首屏展示主推荐、三类补给板块、用户可见标签和同步提示。
- [x] 4.2 根据当前时段和公开配置展示午间、下午续命、通勤等场景主推荐，不用假活动填充空场景。
- [x] 4.3 补给卡片展示场景标题、描述、标签、行动文案、可点击状态和最近补给反馈，继续隐藏 CPS、佣金、返利和平台内部字段。
- [x] 4.4 点击补给时展示准备跳转、补给已打开、备用通道、不可用、环境无法跳转等状态反馈。
- [x] 4.5 确保点击失败、备用链接不可归因或订单未同步时，不增加今日生存消耗、不展示已入账状态。
- [x] 4.6 在账本关联反馈中只展示本人可理解的账本自动收纳状态，不展示平台订单号、完整 `sid` 或后台原因码。

## 5. 后台活动运营

- [x] 5.1 扩展后台补给项表单，支持活动分组、标签、优先级、推荐时段、工作日规则、归因窗口、转链有效期和备用链接策略。
- [x] 5.2 增加补给活动批量上架、批量下线、排序调整和复制活动能力。
- [x] 5.3 增加普通用户可见效果预览，校验预览响应不包含 CPS、聚推客、佣金、返利、完整 `sid` 或内部活动 ID。
- [x] 5.4 后台活动列表支持按板块、标签、状态、有效期、推荐时段和活动来源筛选。
- [x] 5.5 后台活动操作保持现有实时待办和可控刷新边界，编辑中不被自动刷新覆盖。

## 6. 后台 CPS 排查、异常池和指标

- [x] 6.1 新增 CPS 排查中心接口，按订单或点击返回来源活动、脱敏 `sid`、点击、转链、订单同步、入账状态和失败原因链路。
- [x] 6.2 新增“为什么这单没入账”原因码和用户可读解释，覆盖 `sid` 缺失、未匹配、过期、活动不匹配、订单无效、重复订单、分类异常和入账失败。
- [x] 6.3 新增 CPS 异常池接口和后台页面，支持查看、筛选和重试可恢复异常。
- [x] 6.4 确保后台不提供把无归因订单强行分配给任意用户的操作。
- [x] 6.5 新增 CPS 指标接口和后台面板，展示今日点击数、独立点击用户数、转链成功率、订单回流数、有效订单数、入账金额、异常订单数和三类分类统计。
- [x] 6.6 指标和排查接口统一使用字段白名单，过滤聚推客密钥、完整 `sid`、微信身份、用户登录态、完整原始同步响应和佣金返利承诺。

## 7. 验证与文档

- [x] 7.1 扩展 `pnpm verify:supply-center`，覆盖活动标签、分组、排序、公开字段过滤、归因 token 生命周期和转链备用策略。
- [x] 7.2 扩展 `pnpm verify:supply-center-api`，覆盖重复点击、归因窗口、跨活动不匹配、转链失败、备用链接和完整 `sid` 不外泄。
- [x] 7.3 扩展 `pnpm verify:accounting-ledger`，覆盖重复同步、状态变化、退款回滚、无归因异常、分类异常和统计重算。
- [x] 7.4 扩展 `pnpm verify:miniapp-supply-center`，覆盖今日补给面板、时段推荐、空状态、点击状态反馈和用户侧内部字段隐藏。
- [x] 7.5 扩展 `pnpm verify:admin-supply-center` 和 `pnpm verify:admin-operations`，覆盖批量运营、复制活动、排查中心、异常池、指标和敏感字段黑名单。
- [x] 7.6 更新内测人工检查清单，补充 CPS 排查中心、异常订单、退款回滚、指标面板和普通用户合规文案检查。
- [x] 7.7 运行相关验证命令：`pnpm verify:supply-center`、`pnpm verify:supply-center-api`、`pnpm verify:miniapp-supply-center`、`pnpm verify:admin-supply-center`、`pnpm verify:accounting-ledger`、`pnpm verify:admin-operations` 和 `pnpm typecheck`。
