## 1. 共享契约与数据模型

- [x] 1.1 在 `packages/shared` 新增社区发布资格、隐私同意版本、手机号验证状态、身份门槛错误码和 IP 属地公开/后台摘要类型。
- [x] 1.2 扩展社区帖子、评论、回复和公开个人主页响应契约，支持降精度 `ipLocationLabel`，并保持手机号验证状态不进入公开响应。
- [x] 1.3 扩展 Prisma 用户相关模型，保存隐私政策同意版本、同意时间、同意场景、手机号验证状态、验证时间和验证来源。
- [x] 1.4 扩展 Prisma 社区帖子、评论和回复模型，保存发布时 IP 属地快照、解析来源、解析时间和解析失败摘要。
- [x] 1.5 新增数据库迁移和内存 fallback，使未配置数据库时仍可验证隐私同意、手机号验证状态和 IP 属地快照。

## 2. 隐私同意与手机号验证 API

- [x] 2.1 新增或扩展本人社区发布资格接口，返回未同意、未手机号验证、可发布等客户端可识别状态。
- [x] 2.2 新增隐私政策/社区用户协议同意记录接口，保存当前版本、同意时间和同意场景。
- [x] 2.3 新增微信手机号验证服务端接口，接收小程序手机号验证动态 `code`，调用微信接口换取手机号并保存验证状态。
- [x] 2.4 增加手机号验证 mock 配置和错误模拟，支持本地、测试和内测环境验证。
- [x] 2.5 确保服务端不混用微信登录 `code` 和手机号验证 `code`，不向公开响应返回完整手机号或手机号尾号。

## 3. 社区写入口门槛

- [x] 3.1 在发帖接口加入隐私同意、手机号验证和既有用户治理状态统一校验，未满足时不创建待审核帖子。
- [x] 3.2 在评论接口加入隐私同意、手机号验证和既有用户治理状态统一校验，未满足时不创建评论记录。
- [x] 3.3 在回复接口加入隐私同意、手机号验证和既有用户治理状态统一校验，未满足时不创建回复记录。
- [x] 3.4 保持浏览、点赞、收藏不强制手机号验证；保持举报只要求登录和建档，不强制手机号验证。
- [x] 3.5 统一社区身份门槛错误响应，使小程序能区分未登录、未建档、未同意、未手机号验证和治理限制。

## 4. IP 属地归因服务

- [x] 4.1 实现可信客户端 IP 提取工具，支持受控代理 header 配置，并忽略不可信 `X-Forwarded-For` 等可伪造来源。
- [x] 4.2 接入第一版 IP 属地解析方案，生成境内省/自治区/直辖市、境外国家或地区、未知等降精度标签。
- [x] 4.3 在发帖、评论和回复创建成功时保存发布时 IP 属地快照，不接受前端传入 IP 或属地作为事实来源。
- [x] 4.4 处理 IP 识别或解析失败路径，保存 `未知` 或解析失败摘要，且不伪造属地。
- [x] 4.5 确保历史无属地内容不被当前请求或客户端传值补造属地。

## 5. 小程序发布流程

- [x] 5.1 在发帖页提交前接入社区发布资格检查，未同意时展示隐私同意面板。
- [x] 5.2 隐私同意面板展示隐私政策、社区用户协议和小程序用户隐私保护指引入口，且不默认勾选同意。
- [x] 5.3 用户主动同意后再拉起微信手机号验证组件；用户拒绝时不得调用手机号验证或提交内容。
- [x] 5.4 评论和回复入口复用同一发布资格检查、隐私同意和手机号验证流程。
- [x] 5.5 验证成功后返回原发帖、评论或回复流程，并在失败时展示可重试提示，不伪造提交成功。

## 6. 小程序 IP 属地展示

- [x] 6.1 在帖子详情作者信息或发布时间附近展示帖子发布时 `IP属地`，保持弱化样式。
- [x] 6.2 在评论区作者信息区域展示评论发布时 `IP属地`，并检查小屏文本不溢出。
- [x] 6.3 在回复区作者信息区域展示回复发布时 `IP属地`，避免挤压正文和操作入口。
- [x] 6.4 社区帖子列表默认不展示 IP 属地，进入帖子详情后展示。
- [x] 6.5 为后续公开个人主页接入最近一次公开发布 IP 属地展示，且不展示手机号验证状态。
- [x] 6.6 确保提审包和生产包默认开启帖子详情、评论、回复和个人主页 IP 属地展示位，普通用户不可关闭。

## 7. 后台治理与运营摘要

- [x] 7.1 在后台社区帖子治理详情中展示作者手机号验证布尔状态、隐私同意版本、帖子发布时 IP 属地和解析状态。
- [x] 7.2 在后台评论和回复上下文中展示发布时 IP 属地标签和解析状态。
- [x] 7.3 在作者治理摘要中展示最近公开发布 IP 属地、手机号验证布尔状态和社区治理状态。
- [x] 7.4 扩展统一审核队列和实时待办摘要，允许展示降精度 IP 属地和必要合规状态。
- [x] 7.5 确保后台响应、运营事件和治理详情不返回完整手机号、手机号尾号、明文 IP、微信 openid、微信 unionid、微信 sessionKey 或用户登录态。

## 8. 文档、配置与验证

- [x] 8.1 更新隐私政策和社区用户协议，说明手机号验证、IP 属地展示、内容安全审核、举报治理、后台处理和保存边界。
- [x] 8.2 更新环境变量样例，补充手机号验证 mock、隐私政策版本、社区协议版本、可信代理和 IP 属地解析配置。
- [x] 8.3 更新内测运行说明和人工检查清单，覆盖未同意不得拉起手机号验证、未验证不得发布和公开侧不展示手机号验证状态。
- [x] 8.4 扩展 `pnpm verify:community-lite` 和 `pnpm verify:community-lite-api`，覆盖身份门槛、手机号验证、IP 属地快照和敏感字段过滤。
- [x] 8.5 扩展 `pnpm verify:miniapp-community-lite`，覆盖隐私同意面板、手机号验证流程、IP 属地展示位和小屏可读性。
- [x] 8.6 扩展后台和 MVP readiness 验证，覆盖后台脱敏摘要、提审/生产 IP 属地展示默认开启和敏感字段黑名单。
- [x] 8.7 运行 `pnpm typecheck`、相关 `verify:*` 命令和必要构建检查，确认 change 达到可验收状态。
