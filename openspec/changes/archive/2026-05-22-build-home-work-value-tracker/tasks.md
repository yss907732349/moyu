## 1. 首页快照读取与同步

- [x] 1.1 梳理设置页当前写入的 `moyuxia.workProfileSnapshot` 格式，确认首页可复用同一缓存键。
- [x] 1.2 为小程序工作档案服务补充或复用快照响应校验能力，确保首页只消费合法 `WorkProfileSnapshot`。
- [x] 1.3 在首页接入本地快照读取，存在有效快照时首屏进入已配置计算状态。
- [x] 1.4 在首页通过统一工作档案客户端服务后台同步 `/me/work-profile`，成功后更新本地缓存和首页状态。
- [x] 1.5 处理 API 同步失败分支：有本地快照时继续本地计算，无本地快照时展示未配置或加载失败引导。

## 2. 首页工作价值计算状态

- [x] 2.1 移除首页固定 `baseAmount`、演示 `profitPerSecond` 和演示金额自增逻辑。
- [x] 2.2 使用 `calculateWorkValueState({ snapshot, now })` 生成首页工作价值状态。
- [x] 2.3 建立首页显示生命周期刷新机制，页面显示时刷新快照并启动计算计时器。
- [x] 2.4 页面隐藏或卸载时清理工作价值刷新计时器，避免后台继续运行。
- [x] 2.5 将工作状态枚举映射为首页可展示文案和视觉状态。

## 3. 首页 UI 替换

- [x] 3.1 将主金额卡改为展示真实今日已摸金额、每秒收益和工作进度。
- [x] 3.2 未配置工作档案时展示配置引导，不显示演示金额或演示已摸时长。
- [x] 3.3 保留薪资时间设置入口，并确保引导和功能入口都进入 `pages/work-profile/settings`。
- [x] 3.4 移除或重写会让用户误以为是真实数据的演示文案。
- [x] 3.5 保持首页暗黑忍者 RPG 视觉结构、触控尺寸和小屏文字可读性。

## 4. 倒计时与隐藏模式

- [x] 4.1 将静态倒计时数组替换为工作价值状态中的距上班、距下班、距休息日和距发薪日映射。
- [x] 4.2 对不可用倒计时字段展示待配置、待接入或其他非数值占位。
- [x] 4.3 移除硬编码节假日真实倒计时表达，外部日历未接入时不得展示伪真实节日倒计时。
- [x] 4.4 接入隐藏模式状态，遮挡主金额和每秒收益。
- [x] 4.5 为隐藏模式添加低调安全视觉状态，同时保留必要工作状态和设置入口。

## 5. 验证

- [x] 5.1 更新或新增小程序工作档案验证脚本，覆盖首页本地快照消费。
- [x] 5.2 验证 API 失败且本地有快照时首页不回退到演示金额。
- [x] 5.3 验证未配置状态不会展示演示金额、演示时长或演示每秒收益。
- [x] 5.4 验证隐藏模式遮挡主金额和每秒收益。
- [x] 5.5 验证倒计时映射不会拼接空值，且不展示硬编码节假日真实倒计时。
- [x] 5.6 运行 `pnpm verify:miniapp-work-profile`、`pnpm typecheck` 和相关构建检查。
