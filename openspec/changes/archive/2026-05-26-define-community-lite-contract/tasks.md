## 1. 共享契约

- [x] 1.1 在 `packages/shared` 定义社区分区 key、帖子状态、评论状态、举报对象类型、排序方式和错误码。
- [x] 1.2 定义帖子、评论、作者公开身份快照、互动统计、列表/详情/创建/互动/举报响应契约。
- [x] 1.3 定义社区输入校验函数，覆盖标题、正文、图片 key 列表、分区 key、评论内容和举报原因。
- [x] 1.4 添加 `pnpm verify:community-lite` 验证脚本，检查共享枚举、分区 key、状态和隐私字段边界。

## 2. API 数据模型与服务

- [x] 2.1 在 Prisma schema 中新增社区帖子、评论、点赞、收藏、举报和审核相关数据模型。
- [x] 2.2 新增 API 社区模块，接入统一本人上下文，识别登录态和用户成长资料。
- [x] 2.3 实现帖子列表和详情读取，确保普通公开查询只返回 `approved` 内容和公开身份快照。
- [x] 2.4 实现发帖接口，校验登录、隐者档案、分区、阵营专区发帖权限和输入内容，并创建 `pending` 帖子。
- [x] 2.5 实现评论接口，校验登录、隐者档案、帖子公开状态和输入内容，并创建 `pending` 评论。
- [x] 2.6 实现点赞、取消点赞、收藏和取消收藏接口，仅允许操作 `approved` 帖子。
- [x] 2.7 实现帖子和评论举报接口，仅允许举报 `approved` 内容，并记录举报原因。
- [x] 2.8 添加 `pnpm verify:community-lite-api` 验证脚本，覆盖身份门槛、阵营发帖限制、审核过滤和隐私字段。

## 3. 最小人工审核能力

- [x] 3.1 提供最小管理审核接口或后台页面，用于查看 `pending` 帖子和评论。
- [x] 3.2 实现帖子人工审核通过、驳回和隐藏能力，并维护 `approved` 发布时间。
- [x] 3.3 实现评论人工审核通过、驳回和隐藏能力。
- [x] 3.4 确认 AI 或规则检测仅作为辅助标记，不会自动把内容发布为公开状态。

## 4. 小程序社区页面

- [x] 4.1 新增统一社区客户端服务，集中处理 API 请求、响应校验、身份门槛错误和降级提示。
- [x] 4.2 将社区首页从演示数据切换为真实列表读取，保留视觉系统布局和分区 tabs。
- [x] 4.3 实现未登录或未建档用户只浏览推荐列表预览，点击详情、切换分区、发帖、评论、点赞、收藏或举报时跳转“我的”页。
- [x] 4.4 实现阵营专区发帖入口限制，非本阵营用户浏览和互动可用但不能在该阵营专区发帖。
- [x] 4.5 实现发帖页，覆盖标题、正文、图片 key 预留、分区选择和提交后待审核提示。
- [x] 4.6 实现帖子详情页，覆盖正文、作者公开身份、互动按钮、评论列表、评论输入和举报入口。
- [x] 4.7 添加 `pnpm verify:miniapp-community-lite` 验证脚本，检查未登录跳转、分区切换、演示数据移除和隐私字段展示。

## 5. 验收

- [x] 5.1 运行 `openspec validate define-community-lite-contract --strict`。
- [x] 5.2 运行 `pnpm format` 并确认文档和代码格式化。
- [x] 5.3 运行 `pnpm format:check`。
- [x] 5.4 运行 `pnpm lint`。
- [x] 5.5 运行 `pnpm typecheck`。
- [x] 5.6 运行 `pnpm verify:community-lite`、`pnpm verify:community-lite-api` 和 `pnpm verify:miniapp-community-lite`。
- [x] 5.7 运行 `pnpm build:api` 和 `pnpm build:miniapp`。
