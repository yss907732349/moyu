## 1. 视觉模式基础设施

- [x] 1.1 新增小程序视觉模式服务，提供 `workplace | hermit` 默认值、读取、保存、切换和当前模式判断。
- [x] 1.2 将视觉模式本地存储 key 固定为 `moyuxia.visualMode`，无本地值时默认返回 `workplace`。
- [x] 1.3 增加运行时导航栏和原生 tabBar 样式应用函数，按当前模式设置导航栏前景/背景、tabBar 背景、默认文字和选中文字。
- [x] 1.4 将 `pages.json` 默认导航栏和 tabBar 色彩调整为工位模式默认外观，保留首页、社区、我的三个原生 tab。
- [x] 1.5 为页面根节点建立统一模式 class 方案，确保所有页面可挂载工位模式或隐者模式样式。
- [x] 1.6 在 `packages/ui-tokens` 中补齐工位模式与隐者模式的背景、卡片、文字、边框、按钮、状态色、弹层和原生 chrome token。
- [x] 1.7 在全局视觉 CSS 中将工位模式设为默认 token，将现有暗黑 RPG token 收敛到隐者模式。
- [x] 1.8 建立暗色硬编码检查清单，覆盖 `#050508`、`#0b0b12`、`#13131b`、深色渐变、白字、暗边框和暗阴影等工位模式风险点。

## 2. 公共组件与全局样式

- [x] 2.1 适配 `vs-page`、`vs-panel`、`vs-card-raised`、`vs-card-pressable`、`vs-chip`、像素按钮、表单控件和全局状态类的双模式样式。
- [x] 2.2 适配 `PixelHeroFrame`，确保重点像素框在工位模式下不显脏、不压文字，隐者模式保持 RPG 质感。
- [x] 2.3 适配 `VisualStatePanel` 的空状态、加载态、错误态、降级态、按钮和状态插画容器。
- [x] 2.4 适配 `FeatureEntryCard` 的 enabled、locked、coming soon、disabled、hidden 可见状态和按压态。
- [x] 2.5 统一头像框、图片框、icon slot、插画遮罩、占位图、失败图和上传格在工位模式下的边框与底座。
- [x] 2.6 统一按钮、图标按钮、胶囊按钮、chip、badge、分段控件、筛选项、进度条、骨架屏、弹层和固定底栏的模式化状态。
- [x] 2.7 检查所有公共样式不会被页面级 `.vs-page` 暗色变量覆盖回旧暗色。

## 3. 首页与主切换体验

- [x] 3.1 将首页右上角原身份胶囊改为模式切换入口，工位模式显示“隐者模式”，隐者模式显示“返回工位”。
- [x] 3.2 为首页模式切换入口补齐按钮感、按压态、小屏宽度和切换 toast。
- [x] 3.3 适配首页身份区、Slogan、阵营标签、同步提示和首跑引导状态。
- [x] 3.4 适配首页今日已摸金额主卡、金额遮挡、隐藏模式、倒计时、进度条和工作设置入口。
- [x] 3.5 适配首页主图、角色插画、气泡、烟雾反馈和补给气泡在工位模式下的可读性。
- [x] 3.6 适配首页隐者日报入口卡、补给铺入口卡、倒计时卡组和今日生存消耗卡。
- [x] 3.7 确认首页工位模式与隐者模式切换不改变工作档案、隐藏模式、金额计算、feature registry 和补给入口逻辑。

## 4. 社区主链路页面

- [x] 4.1 适配社区首页 Banner、分区 tabs、筛选行、消息入口、发帖入口、发布反馈浮层和空/错/加载状态。
- [x] 4.2 适配社区帖子卡、作者头像/昵称入口、阵营标签、日报引用卡、图片缩略图、点赞/评论/收藏栏和按压态。
- [x] 4.3 适配发帖页标题、说明、引用日报卡、板块 chip、标题输入、正文输入、图片上传格、发布按钮和错误反馈。
- [x] 4.4 适配发帖页隐私同意弹层、协议链接、checkbox、手机号验证弹层、开发模拟按钮和加载态。
- [x] 4.5 适配帖子详情页帖子主体、作者入口、媒体图片、引用日报、互动按钮、IP 属地、评论列表和回复列表。
- [x] 4.6 适配帖子详情页底部评论栏、评论输入框、门槛弹层、举报弹层、举报原因、图片查看外壳和固定底栏。
- [x] 4.7 适配公开个人页骨架屏、隐者名片、头像框、关注按钮、统计行、IP 属地、帖子列表和加载更多。
- [x] 4.8 适配关注/粉丝列表的列表面板、骨架屏、用户行、头像、身份文案、关注/取消关注按钮、空状态和错误态。

## 5. 我的与个人资料链路

- [x] 5.1 适配“我的”页登录态、建档流程、职业/阵营选择卡、创建按钮和反馈文案。
- [x] 5.2 适配“我的”页身份主卡、头像、徽章、昵称编辑按钮、等级经验条、关注/粉丝/签到三项数据和签到按钮。
- [x] 5.3 适配“我的”页工作档案卡、今日生存消耗卡、菜单列表、未读角标、阵营插画卡和所有按压态。
- [x] 5.4 适配“我的角色”页英雄图、头像/身份信息、昵称输入、职业输入、职业类型 picker、阵营 picker、保存按钮和错误态。
- [x] 5.5 适配工作档案设置页薪资、上下班时间、休息段、工作日、发薪日、隐藏模式、picker、switch、保存栏和加载/错误反馈。
- [x] 5.6 确认个人页和工作档案页视觉改动不改变登录、建档、资料保存、签到、阵营切换、工作档案保存和本地快照逻辑。

## 6. 我的社区子页面

- [x] 6.1 适配 `community/my-community-list.css` 中复用的页面背景、列表卡、状态标签、统计、空状态和错误态。
- [x] 6.2 适配“我的帖子”入口页的列表项、说明文字、计数标签、箭头和按压态。
- [x] 6.3 适配“审核中帖子”“已审核帖子”的状态标签、审核说明、帖子摘要、不可查看提示和空/错/加载状态。
- [x] 6.4 适配“我的评论”“我的回复”的内容摘要、关联帖子、审核状态、时间、空/错/加载状态。
- [x] 6.5 适配“收藏帖子”的帖子列表、收藏统计、空状态、错误态和跳转按压态。
- [x] 6.6 适配“论坛消息”列表、未读状态、消息类型、时间、空/错/加载状态和操作反馈。
- [x] 6.7 适配“社区规则”页面的协议正文、标题层级、链接、分割线、滚动区域和小屏可读性。

## 7. 日报与大陆新闻

- [x] 7.1 适配隐者日报首页的封面、板块入口、今日参悟、互动状态、空/错/加载状态和跳转按压态。
- [x] 7.2 适配隐者日报板块列表的筛选、文章卡、封面图、摘要、时间、互动统计和加载更多。
- [x] 7.3 适配隐者日报详情页标题、封面、正文、引用发帖入口、点赞/收藏/分享相关 UI、空/错/加载状态和底部操作。
- [x] 7.4 适配大陆新闻列表的封面、文章卡、状态标签、时间、摘要、空/错/加载状态和加载更多。
- [x] 7.5 适配大陆新闻详情的标题、正文、封面、来源/时间、富文本容器、空/错/加载状态和返回体验。
- [x] 7.6 确认日报和大陆新闻视觉改动不改变内容发布生命周期、互动状态、引用发帖和历史数据兼容逻辑。

## 8. 补给铺、webview 与生存账本

- [x] 8.1 适配补给铺顶部标题、说明、封面图、推荐卡、三类补给分组、section icon、商品卡、标签、按钮和同步/错误提示。
- [x] 8.2 适配补给铺商品跳转、备用通道、按钮 disabled/loading 状态和异常提示，不暴露 CPS 或聚推客内部字段。
- [x] 8.3 适配 webview 外壳的导航栏、加载态、错误态、空白背景、重试提示和外部页面不可控边界。
- [x] 8.4 适配生存账本首页统计主卡、背景图遮罩、三类消耗统计、周报卡、订单列表、分类色、金额、空/错/加载状态。
- [x] 8.5 适配生存账本详情弹层、字段行、关闭按钮、遮罩、边框、按压态和小屏溢出。
- [x] 8.6 适配生存账本记录兼容页或禁用页的说明卡、按钮、提示文案和返回路径。
- [x] 8.7 确认补给铺和账本视觉改动不改变转链、归因、订单同步、退款回滚、账本统计和敏感字段隐藏边界。

## 9. 漫画兼容页与摸鱼工作台例外

- [x] 9.1 适配 `comics/index` 兼容页的背景、说明卡、按钮、空状态和非主 tab 提示。
- [x] 9.2 检查 `stealth-workbench/index` 与默认工位模式的导航栏、页面底色、表格容器、字段行和操作列不冲突。
- [x] 9.3 检查 `stealth-workbench/daily-list`、`daily-detail`、`forum`、`forum-detail` 的表格样式、附件区、图片查看层、空/错/加载状态和小屏可读性。
- [x] 9.4 确认摸鱼工作台页面组不跟随隐者模式变成暗黑 RPG，不引入插画、RPG 卡片、游戏化按钮或装饰动画。

## 10. 文档与验收

- [x] 10.1 更新 `docs/miniapp-global-ui-design-prd.md`，记录工位模式默认、隐者模式切换、全页面全组件适配和摸鱼工作台例外。
- [x] 10.2 更新 `docs/miniapp-visual-illustration-system.md`，记录工位模式下资产可读性适配、icon/tabBar 边界和不重画整套白版资产策略。
- [x] 10.3 更新 `AGENTS.md` 当前事实来源摘要，明确默认视觉已变更为工位模式。
- [x] 10.4 运行 `pnpm typecheck`。
- [x] 10.5 运行 `pnpm build:miniapp`。
- [x] 10.6 运行 `pnpm verify:miniapp-package-size`。
- [x] 10.7 用搜索检查用户端页面中裸露暗色硬编码，确认仅保留在隐者模式选择器、资产遮罩或已说明例外中。
- [x] 10.8 人工检查 320px、375px、414px 宽度下所有注册页面的工位模式截图，确认无暗色断层、文字重叠、按钮不可点或横向溢出。
- [x] 10.9 人工检查隐者模式下普通用户端页面仍保留暗黑 RPG 质感，且模式切换不会改变业务数据。
- [x] 10.10 人工检查 `stealth-workbench/*` 仍保持办公表格视觉例外。
