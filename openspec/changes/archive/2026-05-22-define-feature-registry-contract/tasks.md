## 1. 契约与默认配置

- [x] 1.1 在 `packages/shared` 现有基础约定上定义或扩展 `FeatureEntry`、`FeatureStatus`、`FeaturePlacement`、提示文案和可选解锁条件的数据类型。
- [x] 1.2 统一 `featureKey` 使用 snake_case，并为默认 key 建立共享常量。
- [x] 1.3 建立默认 MVP 本地功能入口配置，覆盖首页、我的页、社区、漫画、补给和未来占位入口。
- [x] 1.4 为 `featureKey`、状态、展示区域、排序和公开路由增加配置校验。
- [x] 1.5 明确普通用户可见字段与内部管理字段的序列化边界。
- [x] 1.6 规定第一版 `locked` 入口只强制 `unlockText`，结构化解锁条件作为可选扩展字段。

## 2. 配置读取与过滤

- [x] 2.1 实现按 `placement` 获取功能入口的本地读取方法，并保持与未来 API 响应一致的数据形态。
- [x] 2.2 过滤普通用户不可见的 `hidden` 条目和内部字段。
- [x] 2.3 按展示区域和排序值稳定返回入口列表。
- [x] 2.4 保留远端配置接入扩展点，确保未来替换数据来源时不改入口组件和点击逻辑。

## 3. 小程序端展示与点击

- [x] 3.1 实现可复用功能入口组件，支持图标、标题、描述、状态角标和禁用样式。
- [x] 3.2 将首页或我的页的至少一个入口区域改为基于 `feature-registry` 渲染，并沿用视觉系统中的功能入口状态样式。
- [x] 3.3 实现统一点击处理：`enabled` 导航，`locked`、`coming_soon`、`disabled` 展示对应提示。
- [x] 3.4 确保非 `enabled` 入口不会进入未开放路由。

## 4. 后台配置边界

- [x] 4.1 定义管理员可配置字段：标题、图标、状态、展示区域、排序和用户可见提示文案。
- [x] 4.2 限制或校验业务路由、内部备注、灰度控制和发布状态等敏感字段。
- [x] 4.3 为未授权用户创建、更新、发布或隐藏功能入口的操作增加拒绝逻辑。

## 5. 验证

- [x] 5.1 覆盖 `enabled`、`locked`、`coming_soon`、`disabled`、`hidden` 状态的单元测试或等价验证。
- [x] 5.2 覆盖按 `placement` 过滤、排序和多区域复用同一 `featureKey` 的测试。
- [x] 5.3 覆盖普通用户响应不包含隐藏条目和内部字段的测试。
- [x] 5.4 覆盖非 `enabled` 入口不会导航到业务页面的前端交互测试或等价验证。
