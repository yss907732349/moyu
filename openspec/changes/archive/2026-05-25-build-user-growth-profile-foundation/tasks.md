## 1. 文档与契约准备

- [x] 1.1 同步 `AGENTS.md` 中当前 OpenSpec 状态，移除 `add-static-holiday-countdown` 仍为活跃 change 的过期描述。
- [x] 1.2 在 `packages/shared` 新增用户成长资料、微信登录、职业类型、阵营、签到、等级经验和 API 响应契约。
- [x] 1.3 修正或迁移现有 `UserFaction` 枚举，使其与键影、水遁、策天、游侠四个产品阵营一致。
- [x] 1.4 新增职业类型到阵营的集中映射函数，并覆盖默认昵称、默认头像 key 和初始成长资源生成规则。

## 2. 后端登录与本人上下文

- [x] 2.1 新增微信登录配置项和环境变量样例，确保样例不包含真实微信密钥。
- [x] 2.2 新增微信登录客户端封装，用于服务端通过微信登录凭证换取微信身份，并支持测试替身。
- [x] 2.3 新增应用登录态签发与校验能力，供小程序后续 `/me/*` 请求识别本人。
- [x] 2.4 改造本人上下文服务，使其优先解析应用登录态，并仅在明确开发环境保留 `temporary-dev-placeholder` fallback。
- [x] 2.5 实现 `POST /auth/wechat-login`，支持新用户创建、已有用户识别和登录失败响应。

## 3. 后端成长资料与签到

- [x] 3.1 新增 Prisma 用户、微信身份、用户成长资料和签到记录模型或等价持久化结构。
- [x] 3.2 新增用户成长资料服务，负责读取资料、创建资料、生成默认昵称头像、初始化成长资源和转换快照。
- [x] 3.3 实现 `GET /me/profile`，返回已有资料快照或资料未创建状态。
- [x] 3.4 实现本人资料创建接口，要求提交职业类型，并保证同一用户只有一份当前有效成长资料。
- [x] 3.5 实现 `POST /me/daily-checkin`，保证同一业务日期幂等，更新奖励、连续签到和资料快照。
- [x] 3.6 将等级经验更新逻辑集中在共享契约或后端服务中，并覆盖经验未升级和升级场景。

## 4. 小程序服务层

- [x] 4.1 新增小程序微信登录与应用登录态客户端服务，封装登录请求、登录态缓存和错误归一。
- [x] 4.2 新增小程序用户成长资料服务，封装读取资料、创建资料、签到、超时处理和响应校验。
- [x] 4.3 新增 `moyuxia.userProfileSnapshot` 本地快照读写与合法性校验。
- [x] 4.4 确保资料服务在 API 不可用但本地存在合法快照时可返回降级结果，并在无缓存时暴露可恢复错误。

## 5. 小程序我的页面

- [x] 5.1 将个人中心页静态演示资料替换为登录态、资料加载态、未创建资料态、错误态和真实资料展示态。
- [x] 5.2 实现首次创建隐者档案面板，用户必须选择职业类型后才能创建成长资料。
- [x] 5.3 创建资料成功后展示默认昵称、阵营、头像 key、等级经验、隐币、能量、连续签到、称号和徽章占位。
- [x] 5.4 实现每日签到第一版交互，展示已签到、可签到、重复签到和签到奖励反馈。
- [x] 5.5 将个人中心功能宫格改为读取 `profile_feature_grid`，复用 feature registry 的导航和阻断提示。
- [x] 5.6 保持个人中心既有视觉系统布局，确保小屏下创建档案面板、资料卡、资源栏和宫格文字不重叠。

## 6. 验证与验收

- [x] 6.1 新增或扩展共享契约验证脚本，覆盖职业映射、默认资料生成、等级经验和响应结构。
- [x] 6.2 新增或扩展 API 验证脚本，覆盖微信登录失败路径、本人资料读取/创建、重复创建和签到幂等。
- [x] 6.3 新增或扩展小程序验证脚本，覆盖本地快照、未创建资料流程、签到交互和 `profile_feature_grid` 消费。
- [x] 6.4 运行 `openspec validate build-user-growth-profile-foundation --strict`。
- [x] 6.5 修改实现后运行 `pnpm format`，并运行 `pnpm format:check` 确认格式通过。
- [x] 6.6 运行 `pnpm lint`、`pnpm typecheck`、`pnpm build:api` 和 `pnpm build:miniapp`。
- [x] 6.7 运行受影响验证脚本，包括新增用户成长资料验证、`pnpm verify:feature-registry`、`pnpm verify:feature-registry-api` 和 `pnpm verify:miniapp-feature-registry`。
