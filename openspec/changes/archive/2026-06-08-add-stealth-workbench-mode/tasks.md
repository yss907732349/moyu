## 1. 入口与路由

- [x] 1.1 在共享 feature registry 契约或默认入口配置中新增 `stealth_workbench_mode`，配置公开标题、描述、展示区域、排序、状态和摸鱼模式工作台公开路由
- [x] 1.2 在小程序 `pages.json` 注册摸鱼模式工作台、表格版日报列表、表格版日报详情、表格版论坛列表和表格版帖子详情页面
- [x] 1.3 在首页或首页快捷入口渲染链路接入 `stealth_workbench_mode`，确保非 `enabled` 状态按 feature registry 拦截或隐藏
- [x] 1.4 确认摸鱼模式不新增底部主 tab，不改变首页、社区、我的三个原生 tabBar 结构

## 2. 表格视觉基础

- [x] 2.1 新增摸鱼模式专用表格样式或组件，覆盖页面背景、表格容器、表头、字段行、列表行、合并单元格、操作列、数字列、时间列和文本按钮
- [x] 2.2 实现统一移动端工作表布局规则：普通行固定高度，操作/数字/时间列稳定宽度，长正文使用合并单元格
- [x] 2.3 实现 320px、375px 和 414px 小屏下不横向滚动、不重叠、不遮挡操作列的响应式规则
- [x] 2.4 确保摸鱼模式样式不使用插画、icon、头像、徽章、阵营图、重点像素框、RPG 面板、深色营地背景、发光和游戏化按钮

## 3. 摸鱼模式工作台

- [x] 3.1 新增摸鱼模式工作台页面，读取本地工作档案快照并复用 `calculateWorkValueState({ snapshot, now })` 展示今日累计、剩余时长、当前状态和今日进度
- [x] 3.2 复用首跑状态和工作档案降级边界，未登录、未建档、未配置或 API 错误时展示办公表格空/错状态，不伪造金额或 ready 状态
- [x] 3.3 在工作台中以表格行展示内部日报和讨论记录入口，并导航到摸鱼模式对应页面
- [x] 3.4 实现工作台秒级刷新和页面隐藏/卸载时清理计时器，保持与普通首页工作价值计算一致

## 4. 表格版日报

- [x] 4.1 新增表格版日报列表页面，复用现有日报客户端服务读取栏目、标题、发布时间、互动摘要和详情入口
- [x] 4.2 新增表格版日报详情页面，复用现有日报详情服务，以字段表和合并单元格展示标题、栏目、发布时间、正文和必要状态
- [x] 4.3 确保表格版日报不展示日报封面插画、栏目 icon、RPG 卡片、普通文章卡视觉或社区化发布入口
- [x] 4.4 覆盖日报加载、空数据和错误状态，使用 `同步中`、`暂无记录`、`数据读取失败` 等办公语义

## 5. 表格版论坛

- [x] 5.1 新增表格版论坛列表页面，复用 `listCommunityPosts` 展示分区、标题、赞数、评论数、收藏数、时间、附件数量和查看操作
- [x] 5.2 新增表格版帖子详情页面，复用 `getCommunityPost` 展示标题、分区、发布时间、互动统计、附件数量、正文、评论和回复
- [x] 5.3 在帖子详情中复用 `setCommunityPostLike` 和 `setCommunityPostFavorite` 实现 `标记` 与 `归档` 操作，并同步更新状态和计数
- [x] 5.4 确保摸鱼模式论坛不渲染发帖、评论、回复、举报、图片上传、消息中心、我的帖子、我的评论、我的回复或收藏管理入口
- [x] 5.5 确保论坛列表不展示图片缩略图，详情不直接渲染图片墙或普通社区图片网格

## 6. 附件快速查看

- [x] 6.1 在表格版帖子详情中以 `附件数量 | N | 查看` 展示图片附件入口
- [x] 6.2 实现附件清单展开层或表格区，每个附件仅展示 `附件 N | 查看`，不显示缩略图
- [x] 6.3 实现单图全屏查看层，不显示标题栏、按钮、保存、转发、评论、举报或说明文案
- [x] 6.4 确保图片查看层打开和关闭无动画，点击屏幕任意位置立即关闭，不支持滑动切图

## 7. 验证与文档

- [x] 7.1 新增或更新 `pnpm verify:miniapp-stealth-workbench-mode` 验证命令，检查页面注册、入口配置、公开路由和 feature registry 状态边界
- [x] 7.2 在验证中检查摸鱼模式页面不引用发帖、评论、回复、举报、图片上传、消息中心和个人社区中心操作
- [x] 7.3 在验证中检查摸鱼模式页面不使用插画、icon、头像、徽章、重点像素框、RPG 面板类名或普通社区卡片类名
- [x] 7.4 在验证中检查附件默认以数量展示、附件清单无缩略图、图片查看层具备任意点击关闭和无动画规则
- [x] 7.5 更新 `docs/miniapp-global-ui-design-prd.md` 和相关视觉说明，记录摸鱼模式作为办公表格视觉例外的规则和验收边界

## 8. 回归检查

- [x] 8.1 运行 `pnpm verify:feature-registry` 和 `pnpm verify:miniapp-feature-registry`
- [x] 8.2 运行 `pnpm verify:miniapp-work-profile`、`pnpm verify:miniapp-daily-content-feed` 和 `pnpm verify:miniapp-community-lite`
- [x] 8.3 运行新增的 `pnpm verify:miniapp-stealth-workbench-mode`
- [x] 8.4 运行 `pnpm typecheck` 和必要的小程序构建检查
