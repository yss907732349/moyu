## 1. 共享契约

- [x] 1.1 在 `packages/shared` 新增账单方向、状态、来源、CPS 来源平台、币种和分类 key 等稳定枚举
- [x] 1.2 定义账单快照、创建请求、更新请求、列表响应、统计响应和校验错误响应契约
- [x] 1.3 定义第一版内置收入/支出分类、展示名、图标 key 和排序
- [x] 1.4 实现金额、分类、发生日期、备注和状态的共享校验函数
- [x] 1.5 实现当日和本月收入、支出、净额统计函数，且只统计本人 `posted` 账单

## 2. API 与持久化

- [x] 2.1 在 Prisma schema 中新增账单持久化模型，包含用户归属、金额、币种、分类、发生日期、状态、来源和可选 CPS 来源字段
- [x] 2.2 新增账本 API 模块、服务和控制器，并复用统一本人上下文识别当前用户
- [x] 2.3 实现创建本人账单接口，校验请求并返回账单快照
- [x] 2.4 实现本人账单列表接口，支持按日期范围和账单方向过滤
- [x] 2.5 实现本人账单更新和作废接口，并拒绝访问或修改他人账单
- [x] 2.6 实现本人当日/本月统计接口，确保草稿、作废和非本人账单不参与统计

## 3. 小程序记账流程

- [x] 3.1 新增统一账本客户端服务，封装账单创建、列表读取、更新、作废和统计读取
- [x] 3.2 新增记账记录页，支持收入/支出切换、分类选择、金额、发生日期和备注输入
- [x] 3.3 新增账单列表或账本概览页，展示本人账单和当日/本月统计摘要
- [x] 3.4 保存账单成功后刷新列表或统计摘要，失败时展示明确错误且不伪造入账成功
- [x] 3.5 将首页底部 tab 上方横向辅助工具横幅改为正式记账入口，点击 `记一笔` 导航到记账记录页
- [x] 3.6 首页如展示账本摘要，必须通过统一账本客户端服务读取正式统计，不使用本地演示计数

## 4. CPS 边界预留

- [x] 4.1 在共享契约和持久化模型中预留通用来源字段，支持未来 `manual` 与 `cps_order`
- [x] 4.2 确保第一版手动账单来源为 `manual`，且不要求 CPS 平台或订单 ID
- [x] 4.3 在代码和文档中保持 CPS 订单不得默认直接写入 `posted` 正式账本的边界

## 5. 验证

- [x] 5.1 新增 `pnpm verify:accounting-ledger`，覆盖共享分类、校验和统计口径
- [x] 5.2 新增 `pnpm verify:accounting-ledger-api`，覆盖本人接口、越权边界、状态过滤和统计响应
- [x] 5.3 新增 `pnpm verify:miniapp-accounting-ledger`，覆盖小程序客户端服务、首页入口和保存失败行为
- [x] 5.4 运行 `openspec validate define-accounting-ledger-contract --strict`
- [x] 5.5 运行 `pnpm format` 和 `pnpm format:check`
- [x] 5.6 运行 `pnpm lint`、`pnpm typecheck`、`pnpm build:api` 和 `pnpm build:miniapp`
