## 1. 共享契约与数据模型

- [x] 1.1 在 `packages/shared` 定义补给铺板块、补给项、公开列表、点击归因、转链响应和聚推客订单归一化契约
- [x] 1.2 扩展 CPS 订单来源和状态映射，支持聚推客统一订单字段到内部生存账本状态的转换
- [x] 1.3 定义第一期补给活动白名单和分类映射：美团外卖、饿了么、瑞幸、库迪、奈雪、喜茶、滴滴/花小猪、高德打车
- [x] 1.4 新增 Prisma 模型或等价持久化结构，保存补给项、补给点击、聚推客订单同步记录和入账关联
- [x] 1.5 生成 Prisma Client，并确认未配置数据库时仍有开发期内存 fallback 或可验证降级边界

## 2. 后端补给铺能力

- [x] 2.1 新增补给铺模块，提供普通用户补给铺公开列表 API
- [x] 2.2 新增补给点击 API，校验登录态和隐者档案，生成不可反推用户身份的 `sid`
- [x] 2.3 实现聚推客活动转链服务，服务端读取 `JUTUIKE_API_KEY` 并禁止小程序端直接持有密钥
- [x] 2.4 将转链结果归一化为小程序可执行的跳转目标，优先返回目标小程序 `app_id` 和 `page_path`
- [x] 2.5 处理转链接口失败、超时和不可用响应，返回可理解错误并记录点击状态

## 3. 聚推客订单同步与账本入账

- [x] 3.1 实现聚推客统一订单数据拉取、后台触发或导入入口，并保存原始同步摘要和失败原因
- [x] 3.2 使用 `sid` 匹配补给点击和用户，无法匹配时不得写入任意用户账本
- [x] 3.3 将聚推客订单号、金额、付款时间、状态、活动和品牌信息归一化为生存账本导入输入
- [x] 3.4 复用或扩展现有生存账本导入逻辑，保证同一订单重复同步只更新不重复入账
- [x] 3.5 按补给项分类优先、订单品牌和标题兜底的规则归类到 `隐者食堂`、`下午续命` 或 `通勤`
- [x] 3.6 确保未付款、退款、无效、风控、结算失败或未知状态订单不计入今日摘要和本周报告

## 4. 小程序补给铺体验

- [x] 4.1 新增补给铺页面和三类板块布局：`隐者食堂`、`下午续命`、`通勤补给`
- [x] 4.2 接入补给铺客户端服务，通过统一 API 配置读取公开列表和创建点击
- [x] 4.3 实现补给卡片点击流程：创建点击、获取转链结果、跳转目标小程序或可用落地页
- [x] 4.4 增加补给铺空状态、转链失败状态和订单同步后入账提示，不用演示活动冒充真实补给
- [x] 4.5 确保补给铺、首页生存消耗和账本详情不展示佣金、聚推客字段、订单源 ID 或后台归因字段
- [x] 4.6 将 `supply_center` 入口接入首页快捷入口或补给入口，并保持 feature registry 控制导航

## 5. 后台运营配置

- [x] 5.1 在后台运营中心新增补给铺配置入口
- [x] 5.2 实现补给项列表、新增、编辑、上下架、排序和有效期配置
- [x] 5.3 校验补给项只能归属第一期三类补给板块，并保存默认生存分类和聚推客活动标识
- [x] 5.4 实现补给点击记录排查视图，展示脱敏 `sid`、补给项、活动标识、跳转状态和点击时间
- [x] 5.5 实现聚推客订单同步排查视图，展示订单号、活动标识、品牌标识、匹配状态、入账状态和失败原因
- [x] 5.6 确保所有补给铺运营接口继续使用 `x-admin-token`，并过滤密钥、微信身份字段和用户登录态

## 6. 功能注册表与配置

- [x] 6.1 将默认功能注册表中的 `supply_center` 调整为可开放入口，并配置补给铺公开路由
- [x] 6.2 确保 `supply_center` 非开放状态时小程序阻断或隐藏入口，不通过硬编码绕过
- [x] 6.3 增加环境变量样例和文档占位，说明 `JUTUIKE_API_KEY` 只允许服务端配置

## 7. 验证

- [x] 7.1 新增或扩展补给铺共享契约验证，覆盖板块、活动白名单、公开响应字段和 `sid` 脱敏规则
- [x] 7.2 新增或扩展 API 验证，覆盖补给列表、点击归因、转链失败、聚推客订单同步和账本入账
- [x] 7.3 新增或扩展小程序验证，覆盖补给铺三板块、入口导航、空状态、点击失败状态和隐私字段过滤
- [x] 7.4 新增或扩展后台验证，覆盖补给项配置、令牌校验、同步排查和敏感字段边界
- [x] 7.5 运行 `pnpm typecheck`
- [x] 7.6 运行 `pnpm lint`
- [x] 7.7 运行相关验证命令：`pnpm verify:feature-registry`、`pnpm verify:accounting-ledger`、`pnpm verify:miniapp-accounting-ledger`、`pnpm verify:admin-operations` 以及新增补给铺验证命令
