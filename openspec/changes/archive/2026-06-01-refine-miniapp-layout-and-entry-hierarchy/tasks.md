## 1. 资产、字体与视觉基线

- [x] 1.1 建立小程序用户端插画预留清单，覆盖首页、社区、我的、补给铺、日报、大陆新闻、生存账本、工作设置、首跑、空状态和错误态。
- [x] 1.2 确定正式像素字体选型，记录字体名称、来源、授权、文件位置、使用范围和 fallback 策略。
- [x] 1.3 接入正式像素字体文件或字体加载配置，并在全局样式中定义标题、数字、短标签和正文的字体层级。
- [x] 1.4 建立插画和图标命名规范、预留尺寸和引用映射，确保头像、徽章、阵营插画、banner、功能 icon、菜单 icon、空状态图和错误态图使用稳定 key。
- [x] 1.5 更新 `packages/ui-tokens` 或小程序视觉 token，统一背景、面板、描边、按钮、字体、图标容器、插画容器和状态色。
- [x] 1.6 将用户端默认视觉 token 调整为深色夜幕背景、暗色卡片、细描边、克制辉光和米白正文，保留金/紫/蓝/青/橙等 RPG 状态强调色。
- [x] 1.7 扫描用户端 `px-*`、`placeholder`、`200×200`、无语义圆点 icon 和开发说明文案，形成待替换为预留位或风格化 fallback 的列表。

## 2. 入口配置与共享契约

- [x] 2.1 调整 `packages/shared` 默认 feature registry，使首页不再展示 `hide_mode` 独立快捷入口，并隐藏未定义完整玩法的成就、商店、排行等未来态入口。
- [x] 2.2 调整个人中心常用入口配置或页面入口来源，确保展示 `我的角色`、`工作设置`、`我的帖子`、`收藏帖子`、`论坛消息`、`生存账本`。
- [x] 2.3 检查 `hide_mode` 相关共享类型和工作档案兼容逻辑，保留历史字段读取兼容但不再作为设置页主流程字段。

## 3. 首页布局与生产插画

- [x] 3.1 更新首页工作价值主卡，展示今日已摸金额、工作状态、秒级距离下班倒计时和今日进度条。
- [x] 3.2 移除首页主卡中的”约等于多少时间”和”今日进度百分比”等重复解释型主文案。
- [x] 3.3 在今日已摸金额旁新增小眼睛隐私开关，点击后隐藏金额类敏感信息并可再次恢复。
- [x] 3.4 将首页主体 banner 与隐藏状态联动，普通状态展示工作价值氛围，隐藏状态展示低调修行或安全视觉状态。
- [x] 3.5 为首页普通状态 banner、隐藏状态 banner、日报入口图、补给入口图、倒计时 icon 和今日消耗 icon 接入稳定预留位、asset key 和风格化 fallback。
- [x] 3.6 收敛首页核心快捷入口，保留日报、社区、工作设置、补给相关真实可用入口，移除低价值、重复或未来态按钮。
- [x] 3.7 将首页今日生存消耗改为一张大卡，三列平分展示 `隐者食堂`、`下午续命` 和 `通勤` 金额，并提供右上角 `查看详情`。
- [x] 3.8 确认首页今日生存消耗卡点击后进入生存账本详情页，不进入手动记账页或旧记录页。

## 4. 工作档案设置页调整

- [x] 4.1 从工作档案设置页移除隐藏模式开关 UI。
- [x] 4.2 确认工作档案设置页仍覆盖月薪、上班时段、休息段、工作日规则和发薪日。
- [x] 4.3 重构工作档案设置页表单视觉，接入正式字体、RPG 面板、统一开关、分段控件和生产 icon。
- [x] 4.4 确认保存工作档案时不会因移除隐藏模式 UI 破坏现有请求结构、默认值或本地快照更新。

## 5. 我的页内容补强与阵营资产

- [x] 5.1 下线”我的”页成就首屏占位和 `我的成就` 常用入口，避免展示空徽章、空成就或未定义玩法。
- [x] 5.2 将原成就区域替换为本人状态摘要，展示非月薪的工作档案摘要，并提供工作设置入口。
- [x] 5.3 在本人状态摘要中展示今日生存消耗轻量摘要，并提供生存账本入口。
- [x] 5.4 保留下半区左侧入口列表与右侧阵营插画卡排版，不重做整体布局。
- [x] 5.5 为下半区每个入口配置语义 icon，避免所有入口使用相同无语义圆点。
- [x] 5.6 为四阵营头像、四阵营徽章、四阵营插画和菜单入口 icon 接入稳定预留位、asset key 和风格化 fallback。
- [x] 5.7 在右侧阵营插画卡中保留阵营插画，并可叠加当前阵营修行状态、今日状态或低调态短文案。

## 6. 社区与内容页视觉统一

- [x] 6.1 保持社区页帖子列表、分区、发布和消息业务结构不变，为社区顶部 banner、分区 icon、发布 icon、消息 icon 和默认帖子缩略图接入稳定预留位、asset key 和风格化 fallback。
- [x] 6.2 统一社区帖子卡、作者头像、badge、互动统计和空列表状态的字体、面板和图标风格。
- [x] 6.3 更新日报首页、日报列表、日报详情、大陆新闻列表和大陆新闻详情的字体、封面、正文层级、互动按钮和空状态视觉。
- [x] 6.4 更新生存账本详情页的分类 icon、本周报告视觉、订单空状态、API 错误态和三类金额展示层级。
- [x] 6.5 更新登录、建档、未配置工作档案、无订单、接口错误和 WebView 回退页的生产插画或 icon。

## 7. 补给铺 UI 成熟化

- [x] 7.1 调整补给铺页面标题、说明、板块间距和按钮层级，使其表达上班生存补给工具而非商城首页。
- [x] 7.2 保留 `隐者食堂`、`下午续命`、`通勤补给` 三类板块，并让每个板块的场景说明清晰可扫读。
- [x] 7.3 为三类补给板块插画、补给项默认封面、转链失败图和空板块状态图接入稳定预留位、asset key 和风格化 fallback。
- [x] 7.4 确认补给铺不展示购物车、收货地址、价格墙、券墙、平台促销瀑布流、佣金或返利等电商化/内部商业字段。

## 8. 视觉统一与回归验证

- [x] 8.1 统一首页、社区、”我的”、工作设置、补给铺、生存账本、日报和大陆新闻的暗色背景、暗色卡片、面板间距、字号层级、按钮主次和局部强调强度，避免每个模块都抢主角。
- [x] 8.2 检查小屏宽度下首页金额、倒计时、今日消耗三列、”我的”页入口标题、社区帖子卡、补给铺按钮和表单字段不重叠、不溢出。
- [x] 8.3 检查所有插画预留位上的文字叠加安全区，避免后续正式插画接入后遮挡角色脸部、主体道具或关键场景。
- [x] 8.4 检查正式像素字体在中文、数字、英文、长标题、金额和倒计时场景中的可读性和 fallback 表现。
- [x] 8.5 检查主 tab 和主要二级页恢复全屏深色夜幕作为默认背景，浅色只保留为局部纸张、卷轴、气泡、图标高光或插画道具。
- [x] 8.6 扫描并确认用户端不再出现 `px-* 200×200`、`placeholder`、开发态资产说明或无语义圆点 icon；正式插画未完成处 SHALL 使用风格化 fallback。
- [x] 8.7 运行 `pnpm verify:feature-registry` 和 `pnpm verify:miniapp-feature-registry`。
- [x] 8.8 运行 `pnpm verify:work-profile` 和 `pnpm verify:miniapp-work-profile`。
- [x] 8.9 运行 `pnpm verify:accounting-ledger` 和 `pnpm verify:miniapp-accounting-ledger`。
- [x] 8.10 运行 `pnpm verify:supply-center` 和 `pnpm verify:miniapp-supply-center`。
- [x] 8.11 运行 `pnpm verify:community-lite` 和 `pnpm verify:miniapp-community-lite`。
- [x] 8.12 运行 `pnpm verify:daily-content-feed` 和 `pnpm verify:miniapp-daily-content-feed`。
- [x] 8.13 运行 `pnpm typecheck`。
