## 1. 共享契约与校验

- [x] 1.1 在 `packages/shared` 补充板块级编辑请求、板块级校验结果或复用现有结果所需的类型导出。
- [x] 1.2 提供按 `sectionKey` 过滤日报编辑问题的共享函数，确保阻断项和提示项只归属当前板块或整期设置。
- [x] 1.3 补充板块级发布校验逻辑，确保外部采编来源完整性在目标板块发布前被阻断，其他板块问题不阻断目标板块。

## 2. API 板块级能力

- [x] 2.1 为后台日报控制器增加板块级保存接口，校验 `x-admin-token` 和合法 `sectionKey`。
- [x] 2.2 在日报服务中实现只替换目标 section 的保存逻辑，并确保数据库模式和内存 fallback 行为一致。
- [x] 2.3 增加板块级校验接口或扩展现有校验接口，返回当前板块相关的发布检查结果。
- [x] 2.4 增加板块级预览接口或扩展现有预览逻辑，支持预览今日参悟、栏目入口和栏目文章。
- [x] 2.5 调整 `publish-sections` 发布前校验，使单板块发布只要求目标板块满足发布条件。
- [x] 2.6 覆盖 API 验证：只保存一个板块不改变其他板块；目标板块可发布时不被其他板块问题阻断。

## 3. 后台日报 UI 重构

- [x] 3.1 将日报运营主编辑区从 section tab 改为三个独立板块工作区。
- [x] 3.2 为每个板块工作区提供独立保存、预览、发布和发布检查入口。
- [x] 3.3 调整前端状态管理，板块保存成功后只刷新对应板块和对应检查结果，不清空其他板块未保存输入。
- [x] 3.4 将整期标题、首页摘要、定时发布、整期发布、取消定时和归档收拢到整期设置区域。
- [x] 3.5 保持大陆新闻和离谱卷宗的文章新增、删除、排序、来源字段、图片上传和正文插图能力可用。
- [x] 3.6 保持今日参悟最多一条内容的编辑限制，并在对应工作区展示清晰反馈。

## 4. 验证脚本与回归

- [x] 4.1 更新 `pnpm verify:daily-content-feed-api`，覆盖板块级保存、校验、预览和发布。
- [x] 4.2 更新 `pnpm verify:admin-daily-content-feed`，覆盖三个独立工作区、板块按钮和整期操作分区。
- [x] 4.3 运行 `pnpm typecheck` 并修复类型错误。
- [x] 4.4 运行 `pnpm verify:daily-content-feed-api`。
- [x] 4.5 运行 `pnpm verify:admin-daily-content-feed`。

## 5. 验收

- [x] 5.1 手动确认只修改并保存 `今日参悟` 时，`大陆新闻` 和 `离谱卷宗` 草稿不变。
- [x] 5.2 手动确认只发布 `大陆新闻` 时，`离谱卷宗` 未完成不会阻断发布。
- [x] 5.3 手动确认整期发布和定时发布仍执行整期完整校验。
- [x] 5.4 手动确认小程序公开侧日报摘要、栏目列表和文章详情结构未被破坏。
